- 采用分层架构：API层（FastAPI路由）负责请求处理，Service层（VectorService）封装CLIP模型推理逻辑，Repository层（QdrantRepository）抽象向量数据库操作。
- 依赖注入通过配置中心（pydantic-settings）管理环境变量，支持动态切换模型与数据库连接。
- 入口点为 `app/main.py`，集成CORS中间件并注册 `/api/v1/ai/vector` 路由模块。
- 数据流遵循“请求解析 -> 图像预处理 -> 向量提取 -> 数据库检索/写入”的线性流程，异常处理在各层统一捕获并转换为HTTP状态码。