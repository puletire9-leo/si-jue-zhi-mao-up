- 所有业务逻辑类（如 VectorService, QdrantRepository）均内置 logging 实例，并在关键操作或异常发生时记录结构化日志。
- API接口定义严格遵循 Request/Response 模式，所有输入输出模型均在 `app/models/schemas.py` 中通过 Pydantic BaseModel 统一定义。
- 服务层方法在模型加载失败或推理异常时提供降级策略（如生成模拟向量），确保系统在高负载或依赖不可用时的基本可用性。