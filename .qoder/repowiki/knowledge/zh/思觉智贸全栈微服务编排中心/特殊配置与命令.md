- **一键启动开发环境**：执行 `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` 即可拉起包含 Python、Java 全家桶及前端的全套微服务。
- **生产环境部署**：使用 `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d` 启动精简版生产集群，并通过 `.env` 注入敏感配置。