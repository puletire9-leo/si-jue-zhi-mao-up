- **内存限制启动**：开发环境使用 `node --max-old-space-size=4096` 启动 Vite 以应对大型依赖解析。
- **API 代码生成**：集成 `@hey-api/openapi-ts`，通过 `npm run gen:api` 根据 `openapi.json` 自动生成类型安全的 API 客户端。
- **命名规范检查**：提供 `npm run check-naming` 系列脚本，调用外部 Python 脚本对代码进行命名约定审计。