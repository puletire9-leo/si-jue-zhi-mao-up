- 所有数据库表均包含`created_at`和`updated_at`字段，并使用`CURRENT_TIMESTAMP`及`ON UPDATE CURRENT_TIMESTAMP`自动维护时间戳。
- Kubernetes资源配置中统一使用`app: sjzm-backend`标签进行服务发现与选择器匹配。
- 敏感配置（如数据库用户名/密码）通过Kubernetes Secret (`sjzm-secrets`) 注入环境变量，避免硬编码。