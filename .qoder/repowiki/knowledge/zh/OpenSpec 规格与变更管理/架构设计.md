- 采用 `spec-driven` 规范模式，通过 `.openspec.yaml` 标识变更类型。
- 目录结构分为 `changes/`（包含归档与进行中的变更提案）和 `specs/`（当前生效的基准规格）。
- 每个变更项遵循标准模板：`proposal.md`（提案）、`design.md`（设计）、`tasks.md`（任务清单）及 `specs/`（具体需求场景）。
- 规格文档使用结构化 Markdown，定义 `ADDED/MODIFIED` 需求及 `WHEN/THEN` 验收场景。