- **数据分析**：使用 `polars` 和 `pyarrow` 进行高性能Parquet文件读取与聚合分析（见 `scripts/analysis/generate_liumiao_report_final.py`）。
- **部署编排**：通过 `subprocess` 调用 `kubectl` 实现 Kubernetes 环境的蓝绿部署与金丝雀发布（见 `scripts/deployment/blue_green_deployment.py`）。
- **环境校验**：依赖 `yaml` 和 `hashlib` 进行配置文件哈希比对与依赖版本一致性审计（见 `scripts/env_consistency/core_validator.py`）。