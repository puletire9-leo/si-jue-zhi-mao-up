- 所有数据库操作均通过注入的 `MySQLRepository` 实例执行，并在Celery任务结束时显式调用 `disconnect` 释放连接。
- 任务状态更新采用细粒度策略，每处理完一个文件即调用 `_update_task_status` 同步进度至数据库，确保前端能实时感知。
- 本地缓存文件管理遵循“任务ID隔离”原则，每个任务在缓存目录下拥有独立子目录，任务完成或删除时自动清理临时文件仅保留最终ZIP。
- API接口统一通过 `require_auth` 中间件校验身份，并根据用户角色（管理员/普通用户）动态过滤可访问的任务列表。