- **异步任务队列**：Celery with Redis broker/backend，配置独立队列 `download_tasks` 及自动重试机制（max_retries=3, countdown=60s）。
- **并发下载**：使用 `aiohttp` 实现高并发批量文件抓取，结合 `asyncio.gather` 控制批次并行度。
- **图像处理**：集成 `Pillow (PIL)` 在打包前将图片统一转换为PNG格式以优化兼容性。