- 所有 Repository 类均实现标准的 `connect()` 和 `disconnect()` 方法，并在内部维护连接池或客户端实例。
- Celery 异步任务中若需调用异步 Repository 方法，统一采用 `asyncio.run()` 在同步任务函数内创建临时事件循环执行。
- 配置项统一通过 `app.config.settings` 单例对象访问，禁止在业务代码中直接读取 `os.getenv`。
- 数据库查询方法普遍采用 `execute_query` 配合参数化 SQL，并内置执行时间监控与慢查询日志记录。