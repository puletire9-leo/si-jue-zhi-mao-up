- **AI 视觉栈**：集成 `transformers` (Chinese CLIP) 进行零样本图像分类，使用 `openai` SDK 或 `aiohttp` 调用腾讯云混元大模型 (`hunyuan-vision`) 进行复杂场景理解。
- **云存储**：使用 `qcloud_cos` SDK 对接腾讯云 COS，支持分片上传、签名 URL 生成及多桶策略（如领星专用桶）。
- **图像处理**：基于 `Pillow` (PIL) 实现缩略图生成、格式转换（WebP/PNG）及元数据提取。