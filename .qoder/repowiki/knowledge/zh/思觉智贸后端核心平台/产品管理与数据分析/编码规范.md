- 服务类统一接收 `mysql` 和 `redis` 实例作为构造函数参数，实现数据访问层的解耦。
- API 响应遵循统一的 `{code, message, data}` 结构，或在 FastAPI 中直接返回 Pydantic 模型。
- 数值字段在存入数据库前通常经过 `json.dumps` 序列化（如 tags），读取时进行反序列化处理。
- 缓存键命名遵循 `模块:功能:参数` 的格式（如 `product:{sku}`, `product_data:category_stats:...`）。