- 所有 API 响应均遵循统一的 JSON 结构，包含 `code` (状态码), `message` (提示信息) 和 `data` (业务数据) 字段。
- 数据库操作统一通过 `MySQLRepository` 实例执行，SQL 语句在 Service 或 API 层以字符串形式定义并传入参数化查询方法。
- 敏感操作接口（如用户增删改、配置更新）均通过 `Depends(require_auth)` 注入依赖以实现身份校验。
- 服务类（如 `TokenService`, `BackupService`）通常在模块底部实例化为全局单例对象供外部直接引用。