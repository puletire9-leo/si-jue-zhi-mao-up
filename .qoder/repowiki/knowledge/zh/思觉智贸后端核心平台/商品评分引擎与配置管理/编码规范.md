- API 响应统一采用 `{code, message, data}` 格式，并在成功时返回业务数据，失败时抛出 `HTTPException`。
- Pydantic 模型广泛使用 `Field` 定义别名（如 `dimensionKey`），并通过 `populate_by_name = True` 实现前后端字段命名风格的兼容。
- 服务层方法在执行前检查缓存状态，若为空则异步加载配置，确保业务逻辑与配置数据的解耦。