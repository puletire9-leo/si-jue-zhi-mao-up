- 统一响应格式：所有 API 端点返回包含 `code`, `message`, `data` 字段的字典结构。
- 事务性批量操作：在 Excel 导入及批量删除场景中，显式使用 `begin_transaction`/`commit`/`rollback` 确保数据一致性。
- 动态 SQL 构建：在 Service 层通过列表收集 `where_conditions` 和 `query_params` 动态组装查询语句，支持多条件组合筛选。
- 驼峰与下划线转换：在回收站服务中提供 `dict_to_camel_case` 工具函数，将数据库返回的下划线字段转换为前端所需的驼峰格式。