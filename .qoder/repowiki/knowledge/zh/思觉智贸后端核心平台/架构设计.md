- **应用编排**：在 `app/main.py` 中通过 `lifespan` 钩子并行初始化 MySQL、Redis 和 Qdrant 连接池，并将实例挂载至 `app.state` 供各子模块共享。
- **中间件链**：配置了日志记录、请求超时控制、慢请求监控及大小限制等全局中间件，并集成统一的异常处理机制。
- **路由聚合**：集中注册 `api/v1` 下的所有业务路由（如产品、选品、素材库等），并独立挂载产品销量分析接口。
- **基础设施抽象**：通过 `core_infra` 提供的 Repository 模式屏蔽底层存储差异，实现业务逻辑与数据存储的解耦。