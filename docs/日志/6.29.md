# 工作日志 2026-06-29

## 一、今天完成（已提交 git）

> 当天提交 13 笔（09:25 合并 PR#6 起 → 18:22），主线 master。

### 1. 八爪鱼采集流水线（全天主线，多次迭代）
- **自动化采集流水线落地**（`dcfd342`）：周表 `bazhuayu_weekly_raw` + 流式分页 + 复用初筛 + 前端确认页 `modules/bazhuayu-auto`。
- **接入榜单三国任务**（`c75c72d`）：US/DE/UK taskId，配置文件兜底映射，dev/prod 都从 `config/` 读（`9619de5`）。
- **性能：改流式分页**（`4fc7d63`）解决 40 万行内存堆积。
- **架构调整：改用 notexported+markexported 增量**（`1f8b42e`）解决 192 万历史累积问题。
- **定位修正：不控制云端启停**（`0bd16cb`）——云端已自带周日/周二定时，我们只在周一/周三 06:00 查未导出增量。
- **统一采集控制台**（`f9c3835` + 合并 `ddaa3da`）：
  - 6 任务（榜单×3 + 以图识图×3）启动/停止/实时状态监控
  - 新建 `BazhuayuRunStateService`：CAS 占位防重复启动 + 协作式取消 + 8 态机
  - `BazhuayuClient` 加 `stopExtraction` + 等待进度/取消回调
  - 专用线程池 `bazhuayuExecutor`（core=3/max=6），与卖家精灵池隔离
  - 前端 6 任务控制台，「手动触发」改文案「读取已采数据」
  - **74 单元测试全过，前端构建通过**
- **修复 cron 段数 bug**：prod.env 的 `0 6 * * 1,3`（5 段）会导致 sjzm-product 启动崩溃，已修为 6 段 `0 0 6 * * 1,3`。
- **运维：清理三国历史积压**（US 84.6 万 / DE 75 万 / UK 101.4 万，notexported 全标记已导出）。

### 2. 选品 / 品线
- **选品列表默认走清洗表**（`d94d308`）+ 修复变体数/周批次筛选。
- **郑总同行品线跟随法**接入 AllSelection 筛选抽屉（`9fb2252`）。

### 3. 文档 / 数据
- **领星数据 API 文档 + 客户端脚本**上传（`0a16329`）：`lingxing_api.py` + 13 篇接口文档（产品表现/开发产品/接入指南）。已核实无真实密钥。
- 部署避坑文档 + nginx 路由/Dockerfile 修复（`1b53e2f` / `461ea0c`）。

---

## 二、待验证（代码已上线 master，未部署 / 未联调）

> 八爪鱼控制台、选品改动均已合入 master，但 **env 进镜像需重建 sjzm-product 镜像、前端需用已构建的 vue-dist 部署后才生效**。

1. **八爪鱼控制台真实联调**（谨慎，会动云端 + markexported 不可逆）：
   - dev 环境点「启动云端采集」→ 看 run-state 走 STARTING→WAITING_CLOUD（云端已采数递增）。
   - 中途点「停止」→ 验证云端 stop + 状态置 STOPPED（停止需团队版权限，账号已是团队版）。
   - 完整跑一次 → DRAINING → 周表入库 + 初筛任务产出。
   - 以图识图任一站点启停 → 验证启停/监控通（**本期不验数据管道**）。
2. **积压清完后的定时增量**：周一/周三 06:00 cron 是否能正确拉到云端新批次（积压已清，notexported 应只剩新数据）。
3. **前端构建 OOM**：本次用 `--minify false` 绕过 terser 内存爆，产物不压缩。彻底解决需把 vite minify 从 terser 改 esbuild（待定）。
4. **前端显示变体问题**：新品榜数据库需对接清理后的干净数据源，避免多个变体显示。
5. **变体数 / 周批次筛选**：筛选页面功能问题，需复查（`d94d308` 已修一部分，待验证）。

---

## 三、待开发（新需求）

1. 郑总店铺保存新增功能完善
2. **以图识图数据管道**（本期只做了启停/监控）：补 `updateLoopItems` 写 ASIN 图片 URL + 结果表 `bazhuayu_image_search_result` + `/image-search` 查询接口 + 前端结果页。
3. 1688 数据对接
4. 领星数据对接（文档已上传，业务对接待开发）

---
*日志依据当天 git 提交历史整理，事实来源以 `git log` 为准。*
