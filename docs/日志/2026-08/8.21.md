# 2026-08-21 财务与运营自动化实施收口记录

## 远程选品部署底账更新

- 远程服务器 `8.148.188.93` 已运行首版选品 Docker，正式入口为 `https://selection.suezon.com/ai-selection`。
- 域名解析、服务器外层 Nginx 和 SSL 证书均已配置完成。
- 后续线上更新固定为三步：推送受影响组件的新镜像 tag；修改 `/root/woeau_web/ai-selection-deploy/.env` 中对应的 `*_IMAGE`；进入该目录执行 `docker compose up -d` 拉取新镜像并重启受影响容器。
- `.env` 改的是镜像地址/tag，不修改 `prod-frontend`、`prod-java-product` 等固定容器名。
- SSH 最终配置已打通：本机重新生成无 passphrase 的 `C:\Users\Admin\.ssh\id_ed25519`，指纹 `SHA256:IwVFvKpZA7eWjirq1O9C3vuGYnuYfdu7RXx18uClODw`；公钥已加入服务器 root，`ssh root@8.148.188.93` 实测成功。
- 远程 `docker compose ps/images` 实测可读：业务镜像均为 `v1.0`；`prod-ai-center`、`prod-backend`、`prod-frontend` 健康，`prod-celery-download` 显示 unhealthy，需继续排查。
- 远程“定稿”500 已定位：`prod-backend` 启动时宿主机尚无 `ai-selection` 数据库，重试 10 次后放弃 MySQL 初始化，导致 `/api/v1/final-drafts`、载体和图片设置接口持续报 `'NoneType' object has no attribute 'execute_query'`，健康接口返回 503。当前数据库已导入完成，共 117 张表，`final_drafts` 等关键表均存在；但 backend 自数据库导入后未重启，需重启 backend 后复核。
- `prod-celery-download` 的 unhealthy 是 Compose 复用了 HTTP 8090 健康检查，而 Celery worker 本身不监听 HTTP 端口；日志显示 Redis 连接成功且 worker ready，属于健康检查定义不适用于 worker，并非当前定稿故障根因。

### SSH 密钥重建完整过程

1. 原本的 `id_ed25519` 公钥已在服务器中，但本机私钥设置过 passphrase 且密码遗忘，因此服务器能识别公钥，本机却无法完成签名。
2. 中途创建过临时服务器专用密钥并尝试保存/移除 passphrase，最终未采用；负责人随后清理本机旧密钥文件，以最终配置为准。
3. 在 `C:\Users\Admin\.ssh\` 重新生成 `id_ed25519`，创建时 passphrase 留空；最终公钥为 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINzvaNCIYD/aSNc8yuC/ZYaJfUQdRtxYaesjWH+X2Rhk admin@PS2023LQFKHINO`，指纹为 `SHA256:IwVFvKpZA7eWjirq1O9C3vuGYnuYfdu7RXx18uClODw`。
4. 将最终公钥追加到服务器 root 的 `/root/.ssh/authorized_keys`，并设置目录权限 700、文件权限 600、所有者 root:root。
5. 本机接受服务器 ED25519 主机指纹 `SHA256:Un1lGUNW7m44JhbaCNA3Gwzi9YjAag14x8gYsi/8ckg` 后，`ssh root@8.148.188.93` 成功进入 Ubuntu 20.04 root。
6. Codex 复测 `SSH_OK`，并成功读取 `/root/woeau_web/ai-selection-deploy` 的 `docker compose ps/images`。以后命令行和 MobaXterm 统一使用最终 `id_ed25519`，不再使用中途临时密钥。

## 今日收口

- 已将财务日报、运营物流、自动化中心、领星运行中心、飞书对接中心、人员维度、RDS 批处理写入、数据质量和部署状态统一记录到 [财务与运营自动化任务完整实施记录](../../架构/财务与运营自动化任务完整实施记录.md)。
- 当前财务有效口径确认为：UK+DE 完整请求，领星统一 GBP，分站点白名单过滤，跨站按 ASIN 合并，飞书输出 ALL/GBP。
- 财务自动时间的 SQL 目标值为每日 10:00，实际生产启停需从领星运行中心确认。
- 上架时间口径：统一表 `listing_date`（开发算法 4 信号取最早）优先，缺则 Listing `open_date`，再缺才用产品创建时间。
- 当前不冻结财务状态快照；历史重算必须使用 `pullFromLingxing=false, refreshListing=false`。

## 下午：正式 Java 发布与补数

走唯一脚本 `scripts/deploy/deploy_prod.ps1 -Component java`。预检磁盘 17.78GB 过门。镜像构建、`prod-java-user` / `prod-java-product` / `gateway` 重建均成功；脚本在删除旧 `prod-java:previous` 时因镜像 ID 已不存在退出码 1，**新镜像已在跑**（`prod-java-product` 14:28 起来，SchemaGuard 通过，处理器含 `FINANCE_DAILY_REPORT`）。

补数只走领星请求中心入队，不走沙盒 Python 直写 RDS。

| 日期 | RDS 日事实 | 飞书五表 | 官方任务 |
|---|---|---|---|
| 8.12–8.16 | 各 7059 行 / 6877 ASIN，已有 | 已有（此前口径） | 此前成功 |
| 8.17 | 7059 / 6877，本次写入 | **已发布**。总：SKU 6877，淘汰 2573，断货 231，季节性 13 | `LX_11987eaa2977` 拉数成功、飞书因脏绑定失败；清绑定后 `LX_775df5bfd04f` 17/17 成功 |
| 8.18 | 7059 / 6877 | **已发布**。总：SKU 6877，淘汰 2573，断货 225 | `LX_09edd117f634` 17/17 成功（拉数遇 TLS BUFFER_UNDERFLOW 3 次后恢复） |
| 8.19 | 7059 行日事实；飞书 17/17 | **已发布**（真实 run `f0b580dad9b1481187a56b8a056ceadf` 18:19 SUCCESS） | `LX_2bf9b3d0fd11` SUCCESS |
| 8.20 | RDS 旧日事实和统一表快照已删除，等待完整重拉重新写入 | 此前复用旧 RDS 的 17 行更新成功；替换后的最终投递待完整重拉完成 | 尚未入队，必须等 8.19 终态后再提交 |

### 坑

1. **飞书绑定脏数据**：此前删了飞书 8.17/8.18 行，但 `automation_record_binding` 仍指向旧 `record_id`。正式任务按绑定走更新，飞书回 `1254043 record not found`，五表 17 行全失败。已删 34 条失效绑定后重发成功。代码仍缺「记录不存在则改建」的恢复。
2. **今早 10:00 旧镜像**：`listingTimeSku=0` 拒发飞书。新镜像已用统一表 `listing_date`，8.17 重发通过质量门。
3. **8.18 拉数**：领星 TLS `BUFFER_UNDERFLOW` 重试 3 次后恢复，批次仍完成。
4. **人员维度**：8.17 计算告警未登记/多重匹配开发人、运营；总指标保留，人员行未随机归属。
5. **RDS 与本地库边界**：财务事实实际位于独立 RDS `101.37.51.239/sijuelishi`，不是本地 `prod-mysql`。8.19/8.20 替换操作最终在 RDS 精准删除 `lingxing_product_performance_daily` 和 `lingxing_product_unified_daily` 两表对应日期，各表每天原有 7059 行；没有删除 Listing、飞书绑定或运行审计。
6. **同一任务显示三条运行中**：`d5de04e54f5f4436b7808c5c2d58afe9`、`62e76a9624bb4f46a2804732c4e098ea`、`f0b580dad9b1481187a56b8a056ceadf` 的 `correlation_id` 都是 `lingxing-request:LX_2bf9b3d0fd11`，不是三个并发任务。Docker 两次重启后旧审计未收口。18:19 真实实例 SUCCESS；18:29 两条假 RUNNING 已改为 FAILED/`进程重启中断`。

## 18:29 假 RUNNING 审计收口

- 查询时 `LX_2bf9b3d0fd11` 已是 SUCCESS；真实审计 `f0b580dad9b1481187a56b8a056ceadf` 18:19 SUCCESS，飞书 17 成功 0 失败。
- 两条假审计 `d5de04e54f5f4436b7808c5c2d58afe9`、`62e76a9624bb4f46a2804732c4e098ea` 已改为 FAILED，错误信息「进程重启中断」。没有删除队列任务，也没有再 recover 以免重跑已成功任务。
- 当前 `automation_run` 中 RUNNING 为 0。
- 源码已在 `recoverQueuedTasks` 前增加同前缀遗留 RUNNING 收口；需下次 Java 发布才生效。前端按 `correlation_id` 归组仍待做。

## 文档入口

- 统一实施记录：[财务与运营自动化任务完整实施记录](../../架构/财务与运营自动化任务完整实施记录.md)
- 架构索引：[数据处理中心与自动化中心](../../架构/数据处理中心与自动化中心.md)
- 注册历史：[财务日报自动化任务注册状态](../../架构/财务日报自动化任务注册状态.md)
- 模块入口：`java-backend/AGENTS.md`、`frontend/AGENTS.md`、`backend/AGENTS.md`、`docs/database/README.md`
- 代码入口：`modules/automation`、`modules/dataprocessing`、`modules/lingxing/requestcenter`、`modules/feishu`、`rds`

## 当前阻塞

- 8.19 队列与真实审计已 SUCCESS；假 RUNNING 已收口。8.20 尚未入队，等核对 8.19 RDS/飞书后再提交。
- 启动恢复收口代码尚未随 Java 镜像发布；前端按 `correlation_id` 归组仍待做。
- 飞书更新遇到已删 record 不会自动改建，下次再删飞书行会再踩 `1254043`。
- Docker VHDX 物理占用仍大；本次发布预检已过 15GB，但不等于磁盘问题消失。发布脚本收尾删 `previous` 在镜像 ID 已不存在时会把整次脚本判失败。

## 待办

- [x] 等 8.18 飞书发布结束，确认五表有 8.18
- [x] 8.19 完整拉数 + 落 RDS + 发飞书（`LX_2bf9b3d0fd11` SUCCESS，假 RUNNING 已收口）
- [ ] 8.20 完整重拉 + 替换 RDS + 发飞书（必须等 8.19 终态核对后）
- [x] 自动化中心运行记录展示任务名、报表日、当前步骤、触发来源、数量、耗时和详细状态
- [x] 财务 SKU 去重改为国家+ASIN；每天用当天日事实重算统一表快照后再写飞书
- [ ] 8.19/8.20 完整重拉：先整页落库，按需删除旧 7059 错误行
- [ ] 飞书 `1254043` 时清绑定并改为新建
- [ ] 在自动化中心核对阶段耗时
- [ ] 在领星运行中心核对财务每日 10:00 实际启停
- [ ] 发布脚本删除已不存在 rollback 镜像时不要把整次发布判失败
- [x] 队列启动恢复时收口同 correlation_id 的遗留 RUNNING 自动化审计（源码已改，待发布）
- [ ] 前端按关联任务归组展示执行尝试
