# 2026-08-19 RDS 周链路迁移与财务 UK/DE 分流

## 数据迁移

- 已备份 RDS 统一表、店铺表、财务日事实表和状态快照表。
- 使用 `scripts/migrate_lingxing_weekly_to_rds.py` 将 14 张周链路/模型表幂等迁移到 RDS，全部逐表行数校验通过。
- 最大表 `lingxing_sku_weekly_performance` 迁移 604,442 行；`lingxing_product_performance` 迁移 144,027 行。
- 新建 `lingxing_product_unified_marketplace`，物化 UK 6,573 个 ASIN、DE 862 个 ASIN。

## 财务链路

- 日事实表新增 `marketplace`，已按币种安全回填历史数据：GBP→UK，EUR→DE，未重复请求领星。
- 2026-08-10、08-12、08-14 均已分站点保留在 RDS。
- 状态快照主键升级为 `(snapshot_date, marketplace, asin)`，8.9 历史快照已按统一表关系复制为 UK 6,031 行、DE 855 行，原 `ALL` 副本保留。
- 报表计算按站点隔离，业务键加入 marketplace，禁止两站金额覆盖或相加；生产飞书仅输出 UK/GBP，并按案例动态生成人员维度。
- 飞书 5 张财务表已各新增“国家”和“币种”字段。

## 财务日报 8 月案例逐日回归验证

- 纠正此前测试方法：不再只巡检 RDS 已有日期，改为用案例目录的 08-01、08-02、08-03……标准答案和 ASIN 明细逐日回放。
- `最终给的：理实日报原数据 -飞书.xlsx` 提供 08-01—08-11 的 5 维度标准答案；“第二步”文件提供 08-01—08-09 的 7,270/7,306 条每日 ASIN 输入。
- 旧案例底层每天包含约 947 行 DE 数据，UK+DE 合并后统一显示 GBP；当前 UK-only 规则与旧案例总数天然不一致。
- 案例输出结构为：08-01 共 8 行，08-02—08-03 共 9 行，08-04—08-11 共 16 行；案例只有 7 个开发人，没有夏浩宇。生产现已采用同样的动态规则，夏浩宇仅在当天实际有数据时输出。
- 实际复现断货算法缺陷：案例按“历史曾出单且今天 FBA=0”判断，当前代码只按“昨天断货或今天出单”判断；UK 回放从 08-02 漏 1 个，至 08-09 累计漏 63 个。
- 反例 `B0H1BFVQFP`：08-01 有库存且出单，08-02 变为 0 库存且无销量；案例判定断货，当前状态机漏算。
- 当前案例源明细的标签、创建时间和广告数据与最终飞书答案也存在历史时点差异，需要冻结同批输入/输出夹具。
- 完整结果见 `docs/架构/财务日报8月逐日数据质量验证.md`。

## 安全与幂等

- 目标日期已有日事实时默认拒绝重拉和覆盖；计算、状态落库、飞书投递可分阶段独立执行。
- 周表迁移使用 upsert，不删除 RDS 原数据；生产发布仅使用统一 Java 部署脚本。

## 财务双口径与断货算法修复（16:52）

- 新增 `LEGACY_CASE` 与 `PRODUCTION_UK` 两套显式计算口径：前者合并 UK+DE 复刻案例，后者只处理 UK/GBP。
- 修复断货状态机：从“昨天断货或今天出单”改为“历史曾出单或今天出单”，解决恢复库存后再次断货漏算。
- 历史正销量改为从 RDS 周表 + 既往日事实读取；生产按 marketplace=UK 隔离，案例允许 UK+DE 合并。
- 新增真实反例 `B0H1BFVQFP` 回归测试，并增加案例合并与生产分站结构测试。
- 案例 08-07—08-09 共 21,918 条带缓存公式的 ASIN 行逐行验证，修复公式差异为 0。
- 完整读取生产部署流程并通过预检后，使用 `deploy_prod.ps1 -Component java` 单次缓存构建发布；三个 Java 服务运行正常。
- 本次未创建临时容器、未重复请求领星、未覆盖 RDS 已有事实、未投递飞书。

## 财务生产输出改为案例式动态结构（17:20）

- `PRODUCTION_UK` 不再强制生成 17 行：总、非标品、上架时间共 4 行固定；运营和开发只输出当天实际命中的人员。
- 当前人员名单仍允许 5 名运营和 8 名开发；夏浩宇只有当天实际有归属数据时才输出，不再制造全 0 行。
- 飞书发布门禁同步从 `rows == 17` 改为动态结构校验：总 1 行、非标品 1 行、上架时间 2 行、运营最多 5 行、开发最多 8 行，并拒绝重复维度业务键。
- 完整读取生产部署流程并通过预检后，使用统一 Java 脚本单次缓存构建发布成功；未请求领星、未修改历史事实、未投递飞书。

## 财务人员维度统一配置

- 复用 `person_roster` 作为统一人员来源，增加 `effective_from/effective_to`，按日报日期判断开发和运营人员是否有效。
- 财务 `PRODUCTION_UK` 删除开发/运营硬编码，按目标日期读取人员表；`LEGACY_CASE` 继续使用案例固定名单，避免破坏 08-01—08-11 回归基线。
- 未登记人员或同一 ASIN 多人员匹配时记录 WARN；总指标继续保留，人员维度不随机归属；当天快照继续冻结原始人员字段。
- 新增管理员“人员维度配置”页面，支持四类职能、排序、启停、生效/失效日期和备注维护；删除操作改为停用，避免物理删除历史配置。
- 运营名单补录 5 人；财务生产日期口径中张子轩截至 2026-07-31、夏浩宇自 2026-08-01 生效。
- 飞书发布门禁同步改为按目标日期的有效人员数量和姓名动态校验，不再写死最多 5 名运营、8 名开发，后续新增人员无需再次改算法代码。
- 已备份生产 `person_roster`（4,292 bytes，SHA-256 `01987242D860ECD269F228B24A107FA3C3D87E8C5227E29C1B302DC33583BCCD`），再执行幂等 DDL 和人员补丁。
- Java 与前端均通过统一生产脚本发布；Java `SchemaGuard` 通过，前端容器 healthy，`/person-roster` 返回 200 且命中新页面资源。
- 只读复用 RDS 的 2026-08-12 日事实测试成功：不拉领星、不写事实、不写状态快照、不投飞书，动态输出 17 行，成功 17、失败 0。
- 测试告警发现当前统一表事实中的未登记开发 `贺蓉晖`，以及未登记运营 `唐若/张亚芳/李杉`；总指标保留，人员维度未随机归属，需由管理员确认这些姓名是否加入财务人员配置。
