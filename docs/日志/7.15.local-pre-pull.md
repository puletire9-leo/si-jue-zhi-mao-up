# 2026-07-15

## 今日起点

今天从新的工作节点开始。

前序日志、试验模型和历史输出只作为调查线索，不自动视为今天的正确结论。后续判断必须重新以当前代码、统一基础数据、数据库事实和实际生成结果为准。

## 工作原则

1. 先明确目标、对象粒度、时间范围和计算口径，再修改代码或生成文件。
2. 公共基础数据与模型结果分开维护；模型之间不互相读取结果文件。
3. GBP、EUR 分开计算，不混币种、不直接相加。
4. 无法由现有数据证明的结论，明确标记为“无法判断”，不使用兜底数据伪装精确事实。
5. 每次模型调整都要说明数据来源、公式、适用范围和限制，并用具体样本复核。
6. 输出文件保持少而清楚，按“说明 → 总览 → 分组明细”渐进式披露。

## 今日目标

待本次讨论明确后记录。

## 工作记录

### 1. 创建今日新日志

- 已创建：`docs/日志/7.15.md`
- 状态：新的工作起点。

### 2. 汇总领星两个模型的完整经验

已新增：

```text
产品数据/领星模型/01_领星两个模型思考尝试与经验总结_2026-07-15.md
```

文档集中记录：

- 两个模型各自解决的问题和边界；
- 公共基础统一表、ASIN/SKU、币种、标签和时间口径；
- 月度经营模型的正式口径；
- 生命周期模型的Q1/Q2/Q3数量、利润、成本和回本算法；
- 使用当前SKU池、FBA断货周期、从0累计gross_profit等失败尝试及原因；
- 后续维护流程、禁止事项、数据限制和模型验收清单。

`产品数据/领星模型/00_领星模型目录规则.md` 已增加该经验文档入口。

## 已确认结论

待记录。

## 待处理事项

待记录。
