1.分析这个：智能纪要：竞品店铺运营数据对比分析会 2026年7月7日.md

## 卖家精灵 API 调用口径补档（2026-07-08）

已新增：

```text
docs/卖家精灵api/调用口径与踩坑记录.md
```

记录后续最容易忘、但会直接影响数据口径的细节：

- `marketplace` 当前只按具体国家传 `UK / DE / US`，不存在 `ALL`。
- 统一口径叫“卖家精灵使用次数”；当前每月 20000 次，一次 HTTP 请求算 1 次。
- 店铺名查询抓店铺全集时固定 `variation=Y`，不含变体，对齐亚马逊店铺页父体商品数量。
- 新品榜 / ASIN 补数可以含变体，后续走 `competitor_products_clean` 清洗。
- 店铺全集进入 `shop_products`，不再做 `shop_products_clean`。
- 一次 HTTP 请求 / 一页最多 100 个商品 = 一次卖家精灵请求次数；230 个商品约等于 3 次请求。
- 店铺名查询分页用 `fetched += items.size()` 判断，不用 inserted/affected rows。
- ASIN 自动分页当前最多取 2 页，控制请求次数。
- API 调用日志和月度使用次数统计继续依赖 `competitor_lookup_log`。

同时在 `docs/卖家精灵api/问题与解答.md` 顶部加入入口链接，后续新增卖家精灵能力前先核对这份口径文档。

## 店铺候选池与精品店铺池完整链路计划（2026-07-08）

基于最新讨论，店铺分析主线从“方法卡命中 -> 观察池 -> 抓店铺全集 -> 画像解释”升级为更完整的作战链路：

```text
每周新品批次
  -> 方法卡筛商品
  -> 按店铺聚合候选店铺
  -> 写入店铺候选池
  -> 人工筛选 / 批量确认
  -> 调卖家精灵店铺名 API 抓全集
  -> 抓取成功后进入正式观察 / 画像体系
  -> 从画像中筛精品店铺
  -> 精品店铺池长期维护
  -> 月度/周期复抓全集
  -> 店铺历史对比 + 多店同上分析
  -> 反哺新的选品方法卡
```

已新增完整计划文档：

```text
docs/店铺品级/店铺候选池与精品店铺池完整实施计划.md
```

本次确认的关键点：

- 一批新品至少几万条，方法卡聚合出的店铺可能有几百到上千，前端必须有批次、方法卡、命中数、类目、状态等筛选。
- 方法卡命中的店铺先进入 `shop_candidate_pool` 候选池，不直接进入正式店铺库。
- 只有人工确认或批量确认后，才调用卖家精灵店铺名 API 抓全集，避免无意义消耗请求次数。
- 抓取成功后写 `shop_products`，并保留入库原因、来源批次、来源方法卡、抓取 run。
- 需要新增 `shop_fetch_run` 记录每次店铺全集抓取的来源、原因、请求次数、成功失败。
- 精品店铺需要独立 `shop_premium_pool`，它是可 CRUD 的长期复用池，可单个/批量新增、暂停、淘汰、修改标签和复抓频率。
- `shop_products` 不覆盖历史批次，每次全量抓取都保存不同时间段数据，便于月度对比。
- 后续重点分析：精品店本月新增商品、消失商品、销量等级变化、多家精品店同周期共同上新的商品族。
- 多店同上分析可反哺新方法卡，例如“精品店铺同周期共上法”。

当前已有基础：

- `competitor_products_clean` 支持方法卡消费。
- M01 已能按店铺聚合命中排名。
- `shop_watchlist` 已可记录观察原因。
- `shop_products` 已作为不含变体店铺全集表。
- `shop-collection` 后端和前端已有观察池与全集画像雏形。

下一步优先级：

```text
1. 建 shop_candidate_pool，先闭合“方法卡 -> 候选店铺 -> 确认抓取”。
2. 建 shop_fetch_run，让每次抓取有来源、原因、请求次数和错误记录。
3. 建 shop_premium_pool，支持精品店铺 CRUD 与周期复抓。
4. 增强前端：方法卡找店铺、店铺候选池、精品店铺池、店铺画像图片墙。
5. 再做历史对比和多店同上分析。
```

## 领星 API 对接现状核查（2026-07-08，待测试）

> 起因：核查 `产品数据/领星数据api` 目录下的领星对接实现到哪一步。结论：**代码已实现且较完整（非骨架），但 Java 端生产联调仍未真跑通，待本次测试。**

### 一、代码分两块

**1. `产品数据/领星数据api/`（Python 蓝本 + 官方文档）**
- `lingxing_api.py`：换 token、签名（MD5→大写→AES-ECB→Base64）的参考实现，Java 版移植蓝本。
- `verify_token.py`：token 实测脚本（凭证走环境变量）。
- `文档/`：领星官方接口文档（店铺列表 / 本地产品 / 产品表现 / 利润统计-ASIN / 错误码）。
- 此目录仅蓝本与文档，业务代码在 Java。

**2. Java `sjzm-product/modules/lingxing/`（真正的实现）**

已打通 4 数据域 + 本地产品写回，全部落库：

| 数据域 | 领星接口 | 落库表 | 幂等键 |
|--------|---------|--------|--------|
| 店铺列表（sid 来源） | `GET /erp/sc/data/seller/lists` | `lingxing_seller` | sid |
| 本地产品（拉取） | `productList` | `lingxing_local_product` | lingxing_id |
| 本地产品（写回） | `product/set` + `uploadPictures` | 同上 | 透传领星 body |
| 产品表现 | `asinList`（≤92天） | `lingxing_product_performance` | 维度组合键 |
| 利润统计-ASIN | `profit/statistics/.../asin/list`（≤7天） | `lingxing_profit_asin` | asin\|sid\|dataDate\|currency |

- 签名/token 在 `LingxingClient.java`：过期前 60s 自动刷新，refresh 失败重换，签名与 Python 字节级一致。
- 控制器 `LingxingController.java`：`/api/v1/modules/lingxing/*`（ping / credentials / call + 4 数据域各自 sync + list）。
- 前端 4 个即插即用模块页：`frontend/src/modules/lingxing-{sellers,products,performance,profit}/`，menuGroup「领星」。
- 落库范式：结构化业务列 + `raw_json` 整包留底，组合业务键手动幂等 upsert（张总蓝本）。

### 二、日志/提交出处

- `docs/日志/6.30.md` 第〇·二节 = 起点：token+签名链路实测打通（换 token `code:200`、关键词列表 `code:0`），Java 基础模块编译+签名验证过。当时明确「未真跑，未碰生产 jar/容器/数据库」。
- git 提交链：`18cc3f2`（基础）→ `d4d9650`（4 数据域 + 写回，主体）→ `4cc32e4`/`b988682`（补文档）。
- 7.6 / 7.7 / 7.8 日志无领星内容，`d4d9650` 之后无领星提交 → 生产联调至今仍是待办。

### 三、待测试（回家跑，顺序）

1. `mvn install`（**别 clean**）→ 重启 product 容器。
2. 确认生产库 4 张表已建（`java-backend/sql/create_lingxing_*.sql`）。
3. `POST /api/v1/modules/lingxing/ping` 验证链路（换 token + 关键词列表 1 条）。
4. 店铺同步 `POST /sellers/sync` 拿 sid（**是产品表现/利润的 sid 来源，必须先跑**）。
5. 本地产品 / 产品表现 / 利润统计依次 sync + list 校验落库。

### 四、注意事项

- ⚠️ 写回 `productSet` 对 `supplier_quote` 等字段传空会**清空**原数据（前端只提交非空字段规避）。
- ⚠️ 4 个 sync 都会真实调用领星接口，勿反复触发；产品表现多店铺同步 10s/页限流较慢。领星官方口径是接口调用 + 令牌桶限流。
- 未做：张总蓝本的 API 调用日志 / 调用次数监控（credit_count 求和 + caller 归因）。

---

## 店铺分析下一阶段：方法卡命中找店铺 + 店铺数据分表（2026-07-08，计划）

### 一、当前判断

数据基础和店铺画像辅助层已经基本闭合：

- `competitor_products_clean` 已经服务新品榜方法卡筛选。
- 郑总 UK/DE clean no variants 数据已导入系统。
- `shop-profile` 店铺画像、快照、基线、定位已跑通。
- A/B/C/D 销量分级、父体口径、郑总精铺结构画像已经验证。

但最终目标不是“画像好看”，而是：

```text
找到我们需要盯的店铺。
```

因此下一阶段主线回到：

```text
新品方法卡命中商品 -> 按店铺聚合 -> 找到高价值店铺 -> 再抓店铺全集 -> 用画像解释和跟踪。
```

### 二、三条数据线确定

#### 1. 新品榜数据：`competitor_products` / `competitor_products_clean`

定位：

```text
专门承接八爪鱼新品榜数据 + ASIN 卖家精灵补数。
```

口径：

- 可以含变体。
- 后续走 `competitor_products_clean` 去变体。
- M01/M03 等新品榜方法卡优先读 clean 表。
- `m01_active` 只是 M01 当前命中的性能标记，不再扩散成一堆 `mxx_active`。

要看的文件：

- `java-backend/sjzm-product/src/main/resources/mapper/MethodCardMapper.xml`
- `java-backend/sjzm-product/src/main/resources/mapper/CompetitorProductsCleanMapper.xml`
- `java-backend/sjzm-product/src/main/resources/mapper/ShopMethodRankMapper.xml`
- `java-backend/sjzm-product/src/main/java/com/sjzm/product/modules/shoprating/service/ShopMethodRankService.java`
- `java-backend/sql/add_m01_active_flag.sql`
- `java-backend/sql/backfill_m01_active.sql`

#### 2. 店铺观察池：`shop_watchlist`

定位：

```text
记录哪些店铺值得抓，以及为什么值得抓。
```

来源可以是：

- `METHOD_CARD`：如 M01 新品命中多。
- `BASELINE`：如郑总相似、自有优质店相似。
- `MANUAL`：人工加入。
- `CATEGORY`：类目专项观察。

建议字段：

```text
marketplace
seller_name
seller_id
source_type
source_code
reason
hit_count
top_category
status
last_fetch_run_id
created_at / updated_at
```

这张表回答：

```text
这家店为什么进入观察池？
它是因为哪张方法卡、哪个基线、哪个人工判断进来的？
```

#### 3. 店铺全集数据：`shop_products`

定位：

```text
专门承接卖家精灵“店铺名查询”返回的店铺商品全集。
```

关键原则：

```text
字段能力基本复制 competitor_products。
```

不是轻量表。因为店铺画像、店铺商品明细、方法卡解释、店铺上新流都需要和新品榜商品类似的完整字段能力。

建表思路：

```sql
CREATE TABLE shop_products LIKE competitor_products;
```

然后补店铺抓取专用字段：

```text
source_run_id
fetch_source
fetch_reason
watchlist_id
variation_mode
raw_json
first_seen_at
last_seen_at
```

口径：

- 卖家精灵店铺名查询固定 `variation=Y`。
- 即不含变体，直接对齐亚马逊店铺页面父体商品数量。
- 不再做 `shop_products_clean`。
- 但仍需要基础标准化：数值字段、`sales_tier`、`age_bucket/listing_days`、末级类目、`raw_json` 留底。

### 三、后续实现顺序

1. 新增 SQL：`shop_watchlist`、`shop_products`。
2. `shop_products` 先用 `CREATE TABLE LIKE competitor_products` 保证字段不缩水，再补店铺专用字段。
3. 卖家精灵店铺名抓取服务改为写入 `shop_products`，固定 `variation=Y`。
4. 方法卡店铺排名结果写入或同步到 `shop_watchlist`，记录进入原因。
5. 店铺画像 `shop-profile` 后续从 `shop_products` 读取，而不是混读新品榜表。
6. 单店详情展示：
   - 店铺为什么进观察池。
   - 该店命中的方法卡商品。
   - 店铺全集 A/B/C/D 分布。
   - A/ABC/D 类目结构。
   - 后续新增 ASIN 上新流。

### 四、相关文档

今天已更新：

- `docs/店铺品级/竞品店铺品级-方法卡命中数方案.md`
- `docs/店铺品级/店铺画像证据层实施计划.md`
- `docs/理式发展思考/阶段发展.md`

重点看：

- `竞品店铺品级-方法卡命中数方案.md`：确认“最终目标是找店铺”，方法卡命中排名是主线。
- `店铺画像证据层实施计划.md`：确认三条数据线、`shop_watchlist`、`shop_products` 的表定位。
- `阶段发展.md`：确认当前经营阶段是“撑盘子后段 + C 层沉淀初期”，C/D 池不是垃圾池，是测品和沉淀池。

### 五、注意

- `competitor_products` 不要继续承接店铺全集数据。
- 店铺全集数据不要为了“省字段”做成轻量表，后面一定会不够用。
- 原始数据来源要分开，分析证据可以统一。
- 方法卡命中最终应统一进 `method_product_hit` 或等价缓存，不要无限增加 `m03_active/m04_active`。

---

## 店铺分析第二/三条数据线：后端 + 前端已实现（2026-07-08，待测试）

> 承接上节计划，`shop_watchlist` + `shop_products` 两条数据线已全量落地。后端 Docker Maven 编译
> `BUILD SUCCESS`（219 源文件），前端 `npm run build -- --minify false` 成功（产物 static/vue-dist/）。
> 均未真实调用/入库验证，待测试。

### 一、建表 SQL（需先执行）

```text
java-backend/sql/create_shop_collection_tables.sql
```

- `shop_products`：`CREATE TABLE LIKE competitor_products`（字段能力完整复制，与 clean 表同范式），
  去掉继承来的 `uk_asin_month`，补店铺专用列 `batch_date/source_run_id/fetch_source/fetch_reason/
  watchlist_id/variation_mode/raw_json/first_seen_at/last_seen_at`，唯一键
  `uk_shop_asin_batch (marketplace, seller_name, asin, batch_date)`。
- `shop_watchlist`：观察池，唯一键 `(marketplace, seller_name, source_type, source_code)`。
- 全部 information_schema 守卫，幂等可重跑。

### 二、后端（Java sjzm-product，新模块 modules/shopcollection）

数据主线：**方法卡命中 → 观察池（记录为什么盯）→ 抓店铺全集 → 画像解释**。

- 实体/Mapper：`ShopProduct`（ASSIGN_ID）+ `ShopWatchlist`（AUTO）+ 各自 upsert；
  `ProductApplication.@MapperScan` 已注册 `modules.shopcollection.mapper`。
- `ShopProductSyncService`：卖家精灵店铺名查询固定 `variation=Y`，写 `shop_products`
  （**不动 deng_zong_shop，郑总盘子保持专用**），落库标准化 sales_tier（A≥100/B≥50/C≥15/D）+
  listing_days + raw_json 留底。
- `ShopWatchlistService.syncFromMethodRank`：调 `ShopMethodRankService.rankByMethod(M01)` 拿排名，
  upsert 进观察池（source_type=METHOD_CARD、hit_count、top_category、reason）；支持人工增删、状态流转。
- 画像读 shop_products：`ShopProfileMapper` 新增 `rankedFromShopProducts` 片段 + 4 个
  `*FromShopProducts` 查询（逻辑与 deng_zong 路径完全一致，仅换表），`ShopCollectionService` 聚合单店全景。
- 控制器 `ShopCollectionController`（`/api/v1/modules/shop-collection/*`）：
  watchlist sync-from-method-rank / list / manual / status / delete；products sync / 原始分页；
  summary / 单店详情 / 单店商品明细。

### 三、前端（3 个即插即用文件，menuGroup「店铺分析」）

- `frontend/src/api/shopCollection.ts`
- `frontend/src/modules/shop-collection-watchlist/`：观察池页（按方法卡刷新、行内抓全集含二次确认、状态流转）
- `frontend/src/modules/shop-collection-shops/`：店铺全集画像列表 + 单店详情抽屉（进池原因 / ABCD / 类目 / 商品明细）

### 四、待测试（回家跑，顺序）

1. 生产库执行 `create_shop_collection_tables.sql`（依赖 `competitor_products` 已存在）。
2. `mvn install`（**别 clean**）→ 重启 prod-java-product 容器；前端产物已在 static/vue-dist/。
3. `POST /watchlist/sync-from-method-rank?methodId=M01&marketplace=UK` → 看观察池是否写入 M01 命中店铺。
4. 前端「店铺观察池」页选一家店「抓全集」（⚠️消耗卖家精灵使用次数）→ 看 shop_products 是否入库、状态转 FETCHED。
5. 「店铺全集画像」页点店铺 → 单店详情抽屉：进池原因 + ABCD 分布 + 类目 + 商品明细分页。

### 五、风险/注意

- ⚠️ 抓全集消耗卖家精灵**使用次数**，前端已加二次确认，勿反复触发。
- ⚠️ M03 店铺排名尚未接（`ShopMethodRankService.rankByMethod` 目前仅 M01，传 M03 抛异常）；
  观察池 sync 现只支持 M01。
- 画像 sales_tier 在 SQL 里用 units 现算，与 shop_products 入库时的 sales_tier 同口径（A≥100/B≥50/C≥15/D），无漂移。
- 未做：`method_product_hit` 统一缓存写入、基线定位接入 shop_products、商品族证据自动聚类。

### 六、代码审查后修正（同日）

审查发现两个会导致数据偏差的问题，已从代码层面修正：

1. `ShopProductSyncService` 店铺全集分页不再用 `inserted >= total` 判断结束。
   - 旧逻辑会把 upsert 更新行也算入 inserted，重复抓或部分更新时可能提前停止。
   - 新逻辑新增 `fetched`，按实际拉取 item 数与 `total` / `pageItems < pageSize` 判断是否继续翻页。
   - 同时 `sellerName` 使用接口返回值，缺失时 fallback 到请求店铺名，避免唯一键和详情查询被空店铺名污染。

2. `ShopMethodRankMapper.selectM01ShopRanking` 不再直接 `COUNT(*)`。
   - 旧逻辑可能把同一父体 ASIN 跨周重复命中算多次，导致观察池 `hit_count` 虚高。
   - 新逻辑先构造 `base`：按 `marketplace + seller_name + parent_asin/asin` 去重，保留最新代表行。
   - `hitCount`、销量等级分布、topCategory 都基于同一批去重后的命中商品计算。

未重新跑 Docker/Maven；仅做代码逻辑修复和 XML 文本解析检查。

### 七、回家开发模式验证流程

> 目标：验证“方法卡命中 -> 观察池 -> 抓店铺全集 -> 店铺全集画像”这条链路真实可用。
>
> 注意：先用 UK + 少量店铺验证，抓店铺全集会消耗卖家精灵使用次数。

#### 0. 先确认代码和数据库环境

```powershell
git pull origin master
```

确认当前工作重点文件：

```text
java-backend/sql/create_shop_collection_tables.sql
java-backend/sjzm-product/src/main/java/com/sjzm/product/modules/shopcollection/
java-backend/sjzm-product/src/main/resources/mapper/ShopMethodRankMapper.xml
java-backend/sjzm-product/src/main/resources/mapper/ShopProfileMapper.xml
frontend/src/api/shopCollection.ts
frontend/src/modules/shop-collection-watchlist/
frontend/src/modules/shop-collection-shops/
```

#### 1. 执行建表 SQL

在开发库执行：

```sql
source java-backend/sql/create_shop_collection_tables.sql;
```

执行后检查：

```sql
SHOW TABLES LIKE 'shop_%';
SHOW CREATE TABLE shop_products;
SHOW CREATE TABLE shop_watchlist;
```

必须确认：

- `shop_products` 存在。
- `shop_watchlist` 存在。
- `shop_products` 有 `source_run_id / fetch_source / fetch_reason / watchlist_id / variation_mode / raw_json / first_seen_at / last_seen_at`。
- `shop_products` 有唯一键 `uk_shop_asin_batch (marketplace, seller_name, asin, batch_date)`。
- `shop_watchlist` 有唯一键 `(marketplace, seller_name, source_type, source_code)`。

#### 2. 启动/重启 Java product

开发模式按你本机习惯执行即可。重点是让 `sjzm-product` 加载新模块：

```text
com.sjzm.product.modules.shopcollection
```

启动后检查日志不要出现：

```text
Invalid bound statement
Table 'shop_products' doesn't exist
Table 'shop_watchlist' doesn't exist
```

#### 3. 验证 M01 方法卡店铺排名仍可用

先验证旧排名接口：

```http
GET /api/v1/modules/shop-rating/method-rank?methodId=M01&marketplace=UK&minCount=1
```

预期：

- 返回店铺列表。
- `hitCount` 是按父体商品去重后的命中数。
- 不应出现明显离谱的跨周重复放大。

如果这里为空，先不要抓店铺全集，先检查：

```sql
SELECT marketplace, COUNT(*)
FROM competitor_products_clean
WHERE m01_active = 1
GROUP BY marketplace;
```

#### 4. 方法卡排名同步到观察池

调用：

```http
POST /api/v1/modules/shop-collection/watchlist/sync-from-method-rank?methodId=M01&marketplace=UK&minCount=1
```

预期返回：

```json
{
  "methodId": "M01",
  "marketplace": "UK",
  "rankedShops": 具体数量,
  "upserted": 具体数量
}
```

数据库核对：

```sql
SELECT marketplace, seller_name, source_type, source_code, hit_count, top_category, status, reason
FROM shop_watchlist
WHERE marketplace = 'UK'
ORDER BY hit_count DESC
LIMIT 20;
```

必须确认：

- `source_type = METHOD_CARD`
- `source_code = M01`
- `reason` 能说明为什么进池。
- `status = WATCHING`

#### 5. 前端观察池页面验证

打开前端：

```text
店铺分析 -> 店铺观察池
```

操作：

1. 选择 UK。
2. 方法卡 M01。
3. 点击“按方法卡刷新观察池”。
4. 看表格是否出现店铺。
5. 看命中数、主打类目、进池原因是否显示正常。

#### 6. 抓一个店铺全集

先选观察池里命中数较高、你愿意消耗使用次数的一家店，点击：

```text
抓全集
```

注意：

- 会触发卖家精灵店铺名查询。
- 请求固定 `variation=Y`，不含变体。
- 一页最多 100 个商品，一页算一次卖家精灵请求次数。

成功后数据库核对：

```sql
SELECT marketplace, seller_name, batch_date, variation_mode, COUNT(*) AS cnt
FROM shop_products
GROUP BY marketplace, seller_name, batch_date, variation_mode
ORDER BY batch_date DESC, cnt DESC
LIMIT 20;
```

必须确认：

- `variation_mode = Y`
- `seller_name` 不为空。
- 同一个店铺有商品入库。
- `raw_json` 有保存。

检查观察池状态：

```sql
SELECT seller_name, status, last_fetch_run_id
FROM shop_watchlist
WHERE marketplace = 'UK'
ORDER BY updated_at DESC
LIMIT 20;
```

抓取成功的店应变成：

```text
FETCHED
```

#### 7. 验证分页没有提前停止

如果卖家精灵返回 `total > 100`，必须确认入库数量接近 total，而不是只有 100。

接口返回里看：

```json
{
  "total": 真实总数,
  "fetched": 实际拉取数,
  "apiCalls": 页数
}
```

判断：

```text
total <= 100  -> apiCalls 应为 1
100 < total <= 200 -> apiCalls 应为 2
200 < total <= 300 -> apiCalls 应为 3
```

如果 `total > 100` 但 `apiCalls = 1`，说明分页仍有问题，需要暂停继续抓取。

#### 8. 验证店铺全集画像列表

调用：

```http
GET /api/v1/modules/shop-collection/summary?marketplace=UK&limit=100
```

或前端打开：

```text
店铺分析 -> 店铺全集画像
```

预期：

- 能看到抓过全集的店铺。
- 商品数、A/B/C/D、ABC 稳定盘、D 测品池有值。
- `variationMode = Y`。

数据库可辅助核对：

```sql
SELECT sales_tier, COUNT(*)
FROM shop_products
WHERE marketplace = 'UK'
  AND seller_name = '填入店铺名'
  AND batch_date = (SELECT MAX(batch_date) FROM shop_products WHERE marketplace = 'UK')
GROUP BY sales_tier;
```

#### 9. 验证单店详情和商品明细

调用：

```http
GET /api/v1/modules/shop-collection/UK/{sellerName}
GET /api/v1/modules/shop-collection/UK/{sellerName}/products?page=1&size=60
GET /api/v1/modules/shop-collection/UK/{sellerName}/products?salesTier=A&page=1&size=60
```

前端验证：

```text
店铺全集画像 -> 点击店铺 -> 抽屉详情
```

必须看到：

- 为什么进观察池。
- 全集画像 A/B/C/D。
- A/ABC/D 类目。
- 商品明细分页。
- ASIN 链接、标题、月销量、销量等级、榜单类目。

#### 10. 异常处理判断

如果卖家精灵返回：

```text
Your IP address is not allowed
```

说明是 IP 白名单问题，不是代码问题。当前外网出口 IP 要加入领星/卖家精灵对应白名单。

如果抓取接口报表不存在：

```text
shop_products doesn't exist
shop_watchlist doesn't exist
```

说明第 1 步 SQL 没执行到当前库。

如果观察池有数据但画像列表为空：

```text
说明还没有抓店铺全集，或 shop_products 当前 marketplace/batch_date 没数据。
```

如果商品数明显大于亚马逊店铺父体数：

```text
检查请求是否仍为 variation=Y；不要用 variation=N。
```

---

## 候选池/精品池完整计划审查修订（2026-07-08）

> 对照已落地代码审查 `店铺候选池与精品店铺池完整实施计划.md`，计划里所有“已实现”声明经代码核实
> 均属实（M01 父体去重、分页 fetched 判断、观察池 upsert 不覆盖 status、sales_tier 三处一致）。
> 但发现 4 个会卡实现/返工的硬伤 + 3 个设计歧义，已全部修订进两份计划文档。

### 已修订的硬伤

1. **候选池 vs 观察池职责冲突**：现有 `syncFromMethodRank` 直写 `shop_watchlist`，与计划“方法卡→候选池→
   确认→观察池”分层冲突。已定边界：方法卡命中只落候选池；观察池收敛为候选确认/人工/基线进入；
   `sync-from-method-rank` 改写候选池；METHOD_CARD 历史数据转过渡态。
2. **`batch_code` 无来源定义 + NULL 陷阱**：已定 = 复用 `ScoringService.getCurrentWeekTag()`（`yyyy-Www`，
   与 `week_tag`/M04 同源），建议提为 `WeekTagUtil` 公共工具；`source_code/batch_code` 均 `NOT NULL DEFAULT ''`
   防唯一键放行多 NULL 导致去重失效。
3. **`shop_products` 字段名对不上**：现状只有 `batch_date`(天)+`source_run_id`，计划早期写 `batch_code`+
   `fetch_run_id`。已定：补 `batch_code` 列(周粒度做历史对比归组)；run 列统一以 `source_run_id` 为准，
   `shop_fetch_run` 主键 `run_id`，各表关联列名随语义但存同一 run_id 值。
4. **`shop_fetch_run` 阶段倒置**：从阶段2 提到阶段1，与候选池同批建。否则阶段1“确认抓取”抓了记不下，
   状态机失败无法追溯。

### 已修订的设计歧义

- 删掉候选池冗余字段 `hit_product_count`（M01 hitCount 已是父体去重值，两者同数）。
- 候选池状态机定为单向不可跳级 `PENDING→SELECTED→FETCHING→FETCHED→PROMOTED`，`PROMOTED` 必须经 `FETCHED`
  （看过画像才进精品池），禁止跳过画像验证。
- 旧「店铺画像」(读 deng_zong_shop) 与新「店铺全集画像」(读 shop_products) 不合并，旧页建议改名
  「郑总基线画像」或收进基线管理，避免双入口混淆。

### 结论

计划链路本身成立，改造方向清楚。落地前只需按上述边界收口，无需推翻。仍待实测：`shop_collection`
第二/三条数据线尚未真实调用/入库验证（测试步骤见本文档前面「店铺分析第二/三条数据线」节）。

### 二审补充硬规则

再次按“准备实现阶段 1”复查计划后，补充以下规则到
`docs/店铺品级/店铺候选池与精品店铺池完整实施计划.md`：

1. **方法卡排名必须按批次过滤**：`sync-from-method-rank` 必须支持 `batchCode / effectiveWeekTag`，
   默认可取当前周，但 SQL 必须落到具体批次，不能拿全历史 `m01_active=1` 排名冒充本周候选。
2. **确认抓取顺序固定**：候选确认时先原子抢锁，再创建/更新 `shop_watchlist` 拿 `watchlist_id`，
   再创建 `shop_fetch_run`，最后抓 `shop_products`。不能抓完后才建观察池，否则商品行无法写 `watchlist_id`。
3. **失败状态补齐**：候选池新增 `FETCH_FAILED`，保留 `fetch_run_id / last_error_message / last_fetch_at`，
   允许从 `FETCH_FAILED` 重试到 `FETCHING`。
4. **批量抓取必须有原子状态锁**：只允许 `PENDING / SELECTED / FETCH_FAILED` 通过
   `UPDATE ... WHERE status IN (...)` 抢到 `FETCHING` 后调用卖家精灵，防止多人操作或重试重复消耗使用次数。
5. **候选同步不能固定 500**：候选池可能几百到上千店铺，后端必须支持分页/可配置 limit，或直接
   `INSERT SELECT` 写候选池。
6. **店铺抓取限速写入计划**：店铺名查询分页和批量抓店铺都按 2 秒/请求限速，最多约 1 分钟 30 次。
7. **前端候选池必须分页**：默认按 `hit_count DESC`，每页 50/100；命中商品图片样例只带 3-6 个，更多懒加载。
8. **统计字段口径更名**：`inserted` 不再解释成新增行，前端展示统一使用 `fetched_count / written_count / failed_count / api_calls`。
9. **同周多次抓取代表快照**：同一 `batch_code` 内多个 `batch_date/source_run_id` 时，周度画像和历史对比默认取最新成功 run，避免重复计数。

### 阶段 2+ 补充审查

继续检查精品池、画像增强、历史对比、多店同上几个后续阶段，补充以下边界：

1. **精品池状态拆分**：`shop_premium_pool.status` 只表示是否属于精品池（`ACTIVE/PAUSED/REMOVED`），
   `refresh_status` 单独表示复抓任务状态（`IDLE/RUNNING/FAILED`），避免一个字段表达两类状态。
2. **精品池软删除**：`REMOVED` 不物理删除；同一 `marketplace + seller_name` 再次加入时恢复原记录，不创建重复精品店。
3. **精品入池前置条件**：除非人工强制加入并立刻抓取，否则必须已存在成功的 `shop_products` 快照；从候选池入精品必须已 `FETCHED`。
4. **精品复抓复用同一抓取链路**：`trigger_type=PREMIUM_REFRESH`，走 `shop_fetch_run`、2 秒限速、原子抢锁和请求次数统计。
5. **复抓计划先手动**：`next_fetch_date` 第一版只是建议复抓日期和筛选条件，不先做自动定时任务，避免批量无控制地消耗使用次数。
6. **画像必须绑定快照**：店铺画像默认展示最新成功快照，但页面必须显示 `batch_code / batch_date / source_run_id`；
   切换批次时指标、商品墙、类目和明细必须同源刷新。
7. **历史对比定义固定**：以 `product_key=COALESCE(parent_asin, asin)` 判断新增、消失、保留；等级变化只在保留商品上计算。
8. **多店同上先可解释**：优先消费商品族证据；没有商品族时使用临时规则键并返回规则说明，不做不可解释黑盒相似。
9. **方法卡线索不直接落正式方法卡**：多店同上结果只能生成“市场信号/方法卡草案线索”，后续人工确认后再升级为正式方法卡。

---

## 第一阶段已实现：候选池闭环 + 抓取运行记录（2026-07-08，待测试）

> 承接审查修订后的计划，阶段 1「候选池 + shop_fetch_run 合并」全量落地。后端 Docker Maven
> `BUILD SUCCESS`（226 源文件），前端 `npm run build -- --minify false` 成功（产物 static/vue-dist/）。
> 均未真实调用/入库验证，待测试。

### 一、新增建表 SQL（需先执行，按顺序）

```text
1. java-backend/sql/add_batch_code_to_shop_products.sql
   —— 给 shop_products 补 batch_code 列（ISO 周 yyyy-Www）+ 索引，information_schema 守卫，幂等可重跑
2. java-backend/sql/create_shop_candidate_tables.sql
   —— 新建 shop_candidate_pool（候选池）+ shop_fetch_run（抓取运行记录）
```

执行后检查：

```sql
SHOW CREATE TABLE shop_products;       -- 必须有 batch_code 列
SHOW CREATE TABLE shop_candidate_pool; -- 必须有 uk_candidate_source (marketplace, seller_name, source_type, source_code, batch_code)
SHOW CREATE TABLE shop_fetch_run;      -- 必须有 run_id 主键
```

### 二、后端（Java sjzm-product，新模块 modules/shopcandidate）

核心链路：方法卡命中 → 候选池（PENDING）→ 人工确认 → 抓店铺全集 → 观察池/精品池。

新增文件：

- `util/WeekTagUtil.java`：ISO 周次单一真相源，`currentWeekTag()` 返回 `yyyy-Www`。`ScoringService.getCurrentWeekTag()`
  已改为委托给它，候选池/竞品/基线共用这一个口子（对齐铁律8）。
- `modules/shopcandidate/entity/ShopCandidatePool.java`：候选池实体（ASSIGN_ID 的表用 AUTO，由 DDL 决定）。
- `modules/shopcandidate/entity/ShopFetchRun.java`：抓取运行记录实体。
- `modules/shopcandidate/mapper/ShopCandidatePoolMapper.java`：`upsert`（ON DUPLICATE KEY UPDATE 不覆盖 status）
  + `atomicLockForFetch`（原子抢锁：仅 PENDING/SELECTED/FETCH_FAILED → FETCHING，affected rows=0 表示抢锁失败）。
- `modules/shopcandidate/mapper/ShopFetchRunMapper.java`：BaseMapper。
- `modules/shopcandidate/service/ShopCandidateService.java`：核心服务。
  - `syncFromMethodRank`：方法卡排名 → upsert 候选池（PENDING），记 batch_code（默认当前周，可传历史周次）。
  - `confirmFetch`：原子抢锁 → 创建/更新 shop_watchlist（source_type=CANDIDATE_CONFIRM）→ 创建 shop_fetch_run（RUNNING）
    → 调 ShopProductSyncService.syncBySellerName → 成功写回 fetch_run=SUCCESS/candidate=FETCHED/watchlist=FETCHED；
    失败写回 fetch_run=FAILED/candidate=FETCH_FAILED + last_error_message。
  - `batchConfirmFetch`：逐条抢锁+抓取，单店失败不阻塞其它。
  - 状态机校验：`PROMOTED` 必须先 `FETCHED`（看过画像才能进精品池），`FETCH_FAILED` 必须从 `FETCHING` 来。
- `modules/shopcandidate/controller/ShopCandidateController.java`：`/api/v1/modules/shop-candidates/*`
  —— sync-from-method-rank / list / {id} / {id}/status / {id}/confirm-fetch / batch-confirm-fetch /
  fetch-runs / fetch-runs/{runId} / manual / {id} DELETE。

修改的已落地代码：

- `ProductApplication.@MapperScan`：注册 `com.sjzm.product.modules.shopcandidate.mapper`。
- `ShopProductSyncService.syncBySellerName`：新增 6 参重载（多 runId + batchCode），保留 4 参旧重载向后兼容。
  抓取时把 `batch_code` 写入 shop_products。`mapToEntity` 同步加 batchCode 参数。
- `ShopProduct` 实体：补 `batchCode` 字段。
- `ShopProductMapper.upsert`：INSERT 列和 ON DUPLICATE KEY UPDATE 都加 `batch_code`。
- `ShopWatchlistService.syncFromMethodRank`：标记 `@Deprecated`（保留向后兼容，新链路走候选池）。
  `ShopCollectionController` 调旧方法会有 deprecation warning（编译通过）。

### 三、前端（3 个文件，menuGroup「店铺分析」）

- `frontend/src/api/shopCandidate.ts`：候选池/抓取记录接口类型 + shopCandidateApi（BASE=`/api/v1/modules/shop-candidates`）。
  方法：syncFromMethodRank / list / getById / updateStatus / confirmFetch（timeout 600000）/ batchConfirmFetch /
  fetchRuns / getFetchRun / addManual / delete。
- `frontend/src/modules/shop-candidate-pool/`：候选池页面（manifest id `shop-candidate-pool`，menuOrder 39）。
  能力：站点/周批次/方法卡/命中数筛选 → 按方法卡刷新候选池；表格 + 多选 + 分页；行内「抓全集」（二次确认）
  +「看画像」+「标记」+「删除」；底部批量确认抓取按钮。状态机限制：只有 PENDING/SELECTED/FETCH_FAILED 可点抓全集。

### 四、待测试（回家跑，顺序）

1. 生产库依次执行两个 SQL（先补 batch_code 列，再建候选池+fetch_run 两表）。
2. `mvn install`（**别 clean**）→ 重启 prod-java-product；前端产物已在 static/vue-dist/。
3. `POST /api/v1/modules/shop-candidates/sync-from-method-rank?methodId=M01&marketplace=UK` → 看候选池是否写入 M01 命中店铺。
   ```sql
   SELECT marketplace, seller_name, source_type, source_code, batch_code, hit_count, status, reason
   FROM shop_candidate_pool WHERE marketplace = 'UK' ORDER BY hit_count DESC LIMIT 20;
   ```
   必须确认：`source_type=METHOD_CARD`、`source_code=M01`、`status=PENDING`、`batch_code` 形如 `2026-W28`。
4. 前端「店铺候选池」页选一家命中数较高的店点「抓全集」（⚠️消耗卖家精灵使用次数，有二次确认）。
   成功后核对：
   - `shop_candidate_pool.status=FETCHED`，`fetch_run_id` 已回填。
   - `shop_fetch_run` 有 SUCCESS 记录，`total/fetched_count/written_count/api_calls` 有值。
   - `shop_watchlist` 有 `source_type=CANDIDATE_CONFIRM` 的记录，`status=FETCHED`，`last_fetch_run_id` 已回填。
   - `shop_products` 该店有商品入库，`batch_code` 有值（不再为空）。
   - `GET /api/v1/modules/shop-candidates/fetch-runs?marketplace=UK` 能查到本次 run。
5. 「店铺全集画像」页点该店 → 抽屉详情能看到全集画像（ABCD/类目/明细）+ 进池原因（CANDIDATE_CONFIRM）。
6. 故意抓一家 IP 白名单外的店测试失败路径：候选池应变成 `FETCH_FAILED`，`last_error_message` 有内容，可重新点「抓全集」重试。

### 五、风险/注意

- ⚠️ 抓全集消耗卖家精灵**使用次数**，前端已加二次确认 + 批量抓取二次确认，勿反复触发。
- ⚠️ 旧 `ShopWatchlistService.syncFromMethodRank` 已 `@Deprecated`，但保留向后兼容；前端新链路走候选池页面，
  旧的「店铺观察池」页面的「按方法卡刷新观察池」按钮暂保留（迁移期并存），后续可移除。
- ⚠️ 候选池状态机单向：`PENDING → SELECTED → FETCHING → FETCHED → PROMOTED`，`PROMOTED` 必须先 `FETCHED`。
  后端 `updateStatus` 强校验，跳级会抛异常。
- ⚠️ `batch_code` 必须复用 `WeekTagUtil.currentWeekTag()`，禁止各模块另写周次算法（铁律8）。
- 未做：批量同步用 INSERT SELECT（当前用循环 upsert，500 家以内可接受）、sample_products_json 命中样例、
  精品池（阶段2）、画像图片墙（阶段3）、历史对比与多店同上（阶段4）。

### 阶段 1 代码审查修复补充

继续复查第一阶段实现后，已补掉几个代码层面的硬问题：

1. `sync-from-method-rank` 必须按 `batchCode/effectiveWeekTag` 过滤，避免把全历史 M01 命中店铺冒充本周候选。
2. `shop_fetch_run.run_id` 与 `shop_products.source_run_id` 已对齐，候选确认抓取时把同一个 runId 传入 `ShopProductSyncService`。
3. 店铺名查询按 2 秒/请求限速；一次 HTTP 请求/一页计为 1 次卖家精灵使用次数。
4. 外部 API 调用不再包在长事务里：抢锁/建 run/成功失败回写走短事务，实际卖家精灵请求在事务外执行。
5. 卖家精灵 API 错误不再吞掉后伪装成功；失败会写 `shop_fetch_run=FAILED`、候选池 `FETCH_FAILED`，并保留本次已消耗的 `api_calls`。
6. 写入失败但接口拉取成功时用 `PARTIAL_SUCCESS` 记录 run，前端按告警态展示 `failedCount`。
7. 统一外部接口口径：卖家精灵叫“使用次数”（20000 次/月），领星按官方文档叫“接口调用 + 令牌桶限流”。

### 卖家精灵请求中心计划补充

今天新增计划文件：

```text
docs/卖家精灵api/卖家精灵请求中心实施计划.md
```

目标是把所有卖家精灵长任务统一放到一个“请求中心”页面看实况：

- ASIN 补数 / 新品榜补数。
- 店铺名抓店铺全集。
- 候选池批量确认抓取。
- 精品店铺月度/周期复抓。

统一口径：

- 卖家精灵统一写“使用次数”，不写容易误导的额度类叫法。
- 当前每月 20000 次。
- 一次真实 HTTP 请求 / 一页 = 1 次使用次数。
- 店铺名查询固定 `variation=Y`，不含变体。
- 店铺名查询每次请求前至少间隔 2 秒。

阶段边界：

- 阶段 1 当前 `batch-confirm-fetch` 保留为同步小批验证入口：逐家执行，底层每次 HTTP 请求仍按 2 秒限速。
- 这个同步入口不能作为几百/上千店铺的大批量正式方案，因为它没有队列实况、暂停、停止、失败 item 重试。
- 大批量抓取进入阶段 1.5：`sellersprite_request_run` + `sellersprite_request_item` + 前端“卖家精灵请求中心”。
  当前第一版由前端/调用方轮询 `consumeNext` 推进，后续再升级后台 worker。
- 候选池页面负责“为什么选这家店、哪些店要抓”；请求中心负责“现在跑到哪、消耗多少使用次数、能不能暂停/停止/重试”。

已顺手修正阶段 1 前端/代码表达，后续又被请求中心桥接替代：

- `ShopCandidateService.batchConfirmFetch` 和 Controller Swagger 标注为“阶段 1 同步小批验证入口”。
- 候选池页面主入口已改为创建 `CANDIDATE_BATCH` 请求中心任务，不再使用同步批量抓取按钮。

---

## 第二阶段已实现：请求中心最小骨架 + 精品店铺池基础闭环（2026-07-08，待测试）

> 承接计划，阶段 2 全量落地。后端 Docker Maven `BUILD SUCCESS`（编译通过），前端 `npm run build -- --minify false` 成功。
> 均未真实调用/入库验证，待测试。三轮审查后修复 4 个硬伤 + 几个中等问题。

### 一、新增建表 SQL（需先执行）

```text
java-backend/sql/create_sellersprite_request_center_tables.sql
  —— sellersprite_request_run（请求中心任务）+ sellersprite_request_item（子项）+ shop_premium_pool（精品池）
```

### 二、后端新增模块

- `modules/requestcenter/`：请求中心最小骨架。
  - `SellerspriteRequestCenterService`：createTask 入队 / dryRunPreview / pause/resume/stop / consumeNext 手动驱动单步消费。
    第一版不做后台线程——调用方（前端轮询/定时任务）按 run 推进。2 秒限速由 `ShopProductSyncService.throttleShopLookup` 保证。
    抓取在事务外，写回用 `TransactionTemplate`。使用次数统计回写到 run.api_calls 和 item.api_calls。
    **消费完成后，若 trigger_type=PREMIUM_REFRESH，自动回写 shop_premium_pool**（refresh_status=IDLE/FAILED + last_fetch_run_id + next_fetch_date）。
  - `retryItem`：FAILED 子项置回 PENDING，run 重开为 RUNNING。
- `modules/shoppremium/`：精品池。
  - `ShopPremiumService`：promoteFromCandidate（前置 FETCHED + 有快照 + 回填 last_fetch_run_id）、addManual（快照校验，forceCreateImmediately 可跳过）、
    update/updateStatus/remove（软删 REMOVED）、refreshDryRun（预览不消耗使用次数）、createRefreshTask（入请求中心，抢锁 ACTIVE+IDLE/FAILED→RUNNING）。
  - 同店已 REMOVED 时 `restoreRemoved` 恢复原行而非新建。status/refreshStatus 双字段不混用。
- `ShopProductMapper` 加 `countByMarketplaceAndSeller`（精品池入池快照校验）。
- `ProductApplication.@MapperScan` 注册 `modules.requestcenter.mapper` + `modules.shoppremium.mapper`。

### 三、前端新增

- `api/shopPremium.ts`：精品池 + 请求中心全部接口类型和方法（含 retryItem）。
- `modules/shop-premium-pool/`：精品池页面（menuOrder 42）。筛选/CRUD/人工加入/dry-run 预览对话框/创建复抓任务/状态流转/软删。
- `modules/sellersprite-request-center/`：请求中心页面（menuOrder 43）。任务列表/推进/暂停/恢复/停止/详情抽屉(子项明细+FAILED 重试)。读 `route.query.runId` 自动开抽屉。
- 入池入口：
  - 候选池页面「入精品池」按钮（仅 FETCHED 可用，默认带 tagsJson `["候选提升"]`）。
  - 画像页抽屉「加入精品店铺池」按钮（走 addManual 快照校验）。
- 精品池创建复抓任务后自动跳转请求中心并带 `runId` query，请求中心自动定位该任务。

### 四、三轮审查与修复

| 严重度 | 问题 | 修复 |
|---|---|---|
| 硬伤 | 请求中心消费后不回写 shop_premium_pool，refresh_status 永久 RUNNING，复抓循环死锁 | `applyPremiumRefreshResult` 在 markItemSuccess/Failed 里按 PREMIUM_REFRESH 触发回写 IDLE/FAILED + last_fetch_run_id + next_fetch_date |
| 硬伤 | promoteFromCandidate 不初始化 last_fetch_run_id/last_fetch_date | 从 candidate.fetchRunId 回填 |
| 硬伤 | 请求中心 FAILED item 无重试入口 | 加 `retryItem` service + `POST /items/{itemId}/retry` + 前端详情抽屉重试按钮 |
| 硬伤 | goToRequestCenter 不带 runId，请求中心无法自动定位 | confirmCreateTask 跳转时带 `query: { runId }` |
| 中等 | 候选池入精品池不带 tagsJson | 默认带 `["候选提升"]` |
| 中等 | addManual forceCreateImmediately 文案写"立即触发抓取"但实际不触发 | 改文案为"后续需手动发起复抓" |

未修(留作后续)：running_count 字段消费过程不递增(前端未展示,不影响功能);dryRunPreview 使用次数估算只按店铺数(下限,大店会低估,标注"下限"即可)。

### 五、待测试（回家跑，顺序）

1. 生产库执行 `create_sellersprite_request_center_tables.sql`。
2. `mvn install`（**别 clean**）→ 重启 prod-java-product；前端产物已在 static/vue-dist/。
3. 候选池页选一家 FETCHED 的店点「入精品池」→ 看精品池有该店，last_fetch_run_id 已回填，候选变 PROMOTED。
4. 精品池页选几家 ACTIVE 的店 → dry-run 预览 → 创建复抓任务 → 自动跳请求中心 → 点「推进」轮询直到终态。
   核对：shop_premium_pool.refresh_status 复抓完回 IDLE，last_fetch_date/next_fetch_date 有值，api_calls 累加。
5. 故意让某条失败（如 IP 白名单外的店）→ 看子项 FAILED + 错误信息 → 点「重试」→ 置回 PENDING → 再推进。
6. 测试暂停/恢复/停止：运行中点暂停，consumeNext 应跳过；恢复后继续；停止后 worker 不再消费。

### 六、风险/注意

- ⚠️ 请求中心 `consumeNext` 是同步接口，前端轮询调用推进。2 秒限速 + 逐条抓取，大批量任务推进较慢——这是有意的，避免无控制地消耗使用次数。
- ⚠️ 精品池复抓走请求中心，**不做同步批量抓取**（计划要求）。dry-run 先预览，确认后才创建任务。
- ⚠️ `next_fetch_date` 按 refresh_frequency 算（WEEKLY+7，MONTHLY+30，MANUAL 不算），第一版只是建议值，无自动定时任务。
- 未做：请求中心后台 worker（当前手动 consumeNext 推进）、running_count 维护、dryRun 按历史 total 精确估算使用次数。
- `docs/店铺品级/店铺候选池与精品店铺池完整实施计划.md` 已补阶段 1.5 请求中心，明确后续实现顺序。

### 七、阶段 2 审查追加修复

继续复核请求中心 + 精品池代码后，又补掉 2 个会影响真实运行的数据问题：

1. `createRefreshTask` 创建 `PREMIUM_REFRESH` 请求中心任务时传入的任务级 `marketplace` 为 `null`，
   但 `sellersprite_request_run.marketplace` 是 `NOT NULL`，会导致任务创建失败。
   已改为：任务级 marketplace 自动从 item 归纳，单站点写 `UK/DE/US`，多站点内部展示写 `MIXED`；
   真正发给卖家精灵的站点仍以每条 item 的 marketplace 为准，不会把 `MIXED` 当 API 参数。
2. 请求中心消费精品复抓 item 时，原来把 `item.triggerId`（精品池 premiumId）传给
   `ShopProductSyncService.syncBySellerName(..., watchlistId, ...)`，会把 `shop_products.watchlist_id`
   写成精品池 ID，污染观察池关联。
   已改为：请求中心复抓不传 watchlistId，精品池关联通过 request item 的 `triggerId` 和
   `shop_premium_pool.last_fetch_run_id` 回写维护。

同步清理：

- `sellersprite_request_item.status` 文档补 `PARTIAL_SUCCESS`。
- 前端精品池文案统一为“使用次数”，不再写容易误导的额度类叫法。
- 前端 `npm run build -- --minify false` 已通过。

### 八、阶段 1+2 逻辑复核追加修复

再次按“候选池 → 精品池 → 请求中心 → 店铺全集 → 状态回写”的整条链路复核，补掉 2 个状态闭环问题：

1. 请求中心 item 为 `PARTIAL_SUCCESS` 时，原逻辑只把 item 标成 `PARTIAL_SUCCESS`，但 run 仍可能因为
   `failed_count=0` 被最终置为 `SUCCESS`，页面会误判为全成功。
   已改为：run 完结时检查是否存在 `PARTIAL_SUCCESS` item；只要存在，run 终态就是 `PARTIAL_SUCCESS`。
2. 精品池复抓任务如果被 `STOPPED`，已经锁定但尚未消费的精品店会一直停在 `refresh_status=RUNNING`。
   已改为：停止任务时只释放尚未开始的 `PENDING` item，标记为 `SKIPPED`，并把对应精品池记录恢复为 `IDLE`；
   正在抓取的 `RUNNING` item 不强行打断，等当前店铺抓完后自然回写结果。

本轮验证：

- 前端 `npm run build -- --minify false` 已通过。
- 文档/代码术语继续统一为“卖家精灵使用次数”。

### 九、请求中心候选批量桥接实现

继续按“所有卖家精灵请求都能进请求中心看实况”的目标复核，补上候选池批量抓取的桥接层：

1. 后端请求中心消费 `CANDIDATE_BATCH` item 时，不再直接调用通用 `ShopProductSyncService` 裸写 `shop_products`。
   已改为调用 `ShopCandidateService.fetchFromRequestCenter`，复用候选池完整收口：
   `atomicLockForFetch` → 创建/更新 `shop_watchlist` → 创建 `shop_fetch_run` →
   店铺全集抓取 → 回写 `candidate/watchlist/shop_fetch_run`。
2. 候选池页面单店“抓全集”和批量抓取都改为创建请求中心任务：
   `requestType=CANDIDATE_BATCH`，`triggerType=CANDIDATE_CONFIRM`，`item.triggerId=candidateId`。
   创建成功后跳转“卖家精灵请求中心”并带 `runId`。
3. 请求中心页面“推进”改为每次消费 1 家店。暂停/停止不打断当前店铺分页抓取，等当前店铺抓完后生效；
   未开始的 item 会停止为 `SKIPPED`，符合协作式控制口径。
4. `batch-confirm-fetch` 后端接口保留为小批验证/兼容入口，但前端主链路不再使用它。

当前边界：

- 精品复抓、候选批量、通用店铺全集已能进入请求中心；`ASIN_LOOKUP` 后续再接。
- 请求中心仍是前端/调用方轮询 `consumeNext` 推进的最小骨架，暂未做后台常驻 worker。

### 十、剩余阶段与实现建议

当前状态：

- 阶段 1：候选池闭环已代码实现，等待真实库/真实卖家精灵请求验证。
- 阶段 1.5：请求中心最小骨架已实现；候选批量和精品复抓已桥接进请求中心。
- 阶段 2：精品店铺池基础闭环已实现，等待真实复抓验证。
- 阶段 3：画像展示增强未开始，是下一步最推荐实现的阶段。
- 阶段 4：历史对比与多店同上未开始，必须等阶段 3 快照一致性稳定后再做。

建议先验收当前链路：

1. 执行 SQL，重启 Java。
2. 候选池创建 `CANDIDATE_BATCH` 请求中心任务。
3. 请求中心推进 1 家店，核对：
   `sellersprite_request_run/item.api_calls`、`shop_fetch_run`、`shop_products.source_run_id`、
   `shop_candidate_pool.status=FETCHED`、`shop_watchlist.status=FETCHED`。
4. 从画像页/候选池把该店加入精品池。
5. 精品池创建复抓任务，推进后核对 `refresh_status` 回到 `IDLE`，`last_fetch_run_id/last_fetch_date/next_fetch_date` 正确。
6. 测暂停/停止：当前店铺抓完后才响应，未开始 item 标 `SKIPPED`，这是可接受口径。

阶段 3 推荐实现顺序：

1. 后端先做快照选择器：`sourceRunId` 优先，其次 `batchCode/batchDate`，都不传取最新成功 `shop_fetch_run`。
2. 画像概览、类目分布、商品明细、商品墙全部绑定同一个 resolved snapshot。
3. 增加店铺快照列表接口，只返回成功/部分成功快照。
4. 增加图片墙接口，按 A 级、ABC 合集、D 级测品、方法卡命中专区分区分页。
5. 前端详情顶部显示当前 `batch_code / batch_date / source_run_id / fetched_count / api_calls`。
6. 前端以图片墙为主视觉，表格只做补充明细。
7. 画像页“加入精品池”继续遵守已抓全集、有成功快照的前置条件。

阶段 4 推荐实现顺序：

1. 先做单店历史对比：指定 baselineRunId 和 compareRunId，计算新增/消失/保留/等级变化。
2. 再做精品池多店同上：只分析 ACTIVE 精品店，先用实时 SQL 和可解释规则。
3. 结果必须能追溯到批次、runId、商品 key 和参与店铺。
4. 第一版只产出“市场信号/方法卡线索”，不要直接写正式方法卡。
