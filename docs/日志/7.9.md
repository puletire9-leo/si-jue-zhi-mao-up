# 2026-07-09 工作区提交收口日志

## 本次提交范围

承接 2026-07-08 的 Docker 开发模式整理、店铺候选池/精品店铺池增强、领星对接、店铺全集画像、商品墙、历史对比和前端导航归档，本次按用户要求将当前工作区全部提交并推送远程。

## 已确认的关键事实

- 店铺总览下已集中店铺相关入口：店铺选品、店铺候选池、店铺全集画像、精品店铺池、请求中心等模块入口已按店铺链路归档。
- 店铺全集画像支持快照选择，`summary/detail/products/categories/product-wall` 使用同一 `sourceRunId` 口径。
- `UK / TUGBA2365 / FETCH_98bc00f5d0e1` 实测商品父体口径为 `519`，已修复同日混批导致的 `555` 偏差。
- 商品墙支持按 A/B/C/D/UNKNOWN 分区并分页返回。
- 单店历史对比已改为 SQL 侧计数和分页，避免全量加载历史商品。
- Docker 开发模式文档和端口配置已整理到当前工作区。

## 提交前验证记录

已在 2026-07-08 修复阶段完成以下验证：

```bash
docker compose -f docker-compose.dev.yml exec -T java-product mvn -pl sjzm-product "-Dtest=ShopCollectionServiceTest,ShopProfileMapperXmlTest" test
docker compose -f docker-compose.dev.yml exec -T java-product mvn -pl sjzm-product -am -DskipTests compile
docker compose -f docker-compose.dev.yml exec -T frontend npx vite build --mode production --minify false --outDir /tmp/sjzm-vite-fix
```

## 后续仍需跟进

- 多店同上分析还未完整实现。
- 精品池多周期复抓后的代表快照规则需要更多真实 run 验证。
- 画像页“加入精品池”的浏览器级交互仍建议继续验收。

## 店铺链路入口清理与生产口径核对

2026-07-09 继续核对生产库和前端页面职责，确认“店铺总览里店铺少”不是数据丢失，而是数据层不同：

- `competitor_products(_clean)` 是新品榜/方法卡候选来源，生产库仍是几万店铺级。
- `shop_candidate_pool` 是当前周方法卡命中的候选池，当前 `2026-W28` 为 294 家（DE 76 / UK 65 / US 153）。
- `shop_products` / `shop_fetch_run` 是确认后卖家精灵店铺全集抓取结果，当前生产为 0，故“店铺全集画像”为空属于当前状态。

为避免前端和后续开发继续混用旧链路，已做清理：

- 前端菜单 `店铺选品` 改为 `方法卡找店`，职责限定为“方法卡命中 -> 候选池 -> 创建请求中心抓取任务”。
- 前端菜单 `店铺观察池` 改为 `正式观察池`，职责限定为“已确认要盯的店铺名单”，不再承担方法卡刷新和直接抓取。
- 正式观察池页面移除“按方法卡刷新观察池”和直连“抓全集”按钮，增加“去方法卡找店”入口和来源筛选。
- 店铺全集画像页面增加空态说明：只展示已抓取成功的 `shop_products` 快照。
- 后端 `/api/v1/modules/shop-collection/watchlist/sync-from-method-rank` 标记旧入口并返回错误，禁止方法卡直写观察池。
- 后端 `/api/v1/modules/shop-collection/products/sync` 标记旧入口并返回错误，禁止绕过请求中心直接抓店铺全集。
- 前端 `shopCollectionApi` 删除旧同步观察池和直连抓取封装。

新的硬边界：

```text
方法卡找店(shop_candidate_pool)
  -> 请求中心(sellersprite_request_run/item)
  -> 店铺全集(shop_products + shop_fetch_run)
  -> 正式观察池(shop_watchlist)
  -> 精品店铺池(shop_premium_pool)
```

## 方法卡找店周批次与跨批重复修复

本次继续修复“方法卡找店”页面的实际使用问题：

- 新增 `GET /api/v1/modules/shop-candidates/method-batches`，专门返回方法卡找店的来源批次。
- M01 来源明确为 `competitor_products_clean.effective_week_tag`，不是候选池已有 `batch_code`，也不是按 `created_at` 临时推导周次。
- 前端“周批次”由手输改为下拉选择，并展示来源表、批次字段、当前批次、命中商品数、命中店铺数。
- 修复生产库旧格式批次 `202606-W00` 按字符串排序抢在 `2026-W28` 前面的问题：来源批次接口改为按 `MAX(created_at) DESC, effective_week_tag DESC` 排序，默认选择最新导入批次。
- “一键全选店铺”已实现为“全选本页可抓店铺”，避免跨页隐式选择造成误操作。
- 跨批重复策略已明确：候选池保留同店不同周记录，因为连续命中是重要证据；批量创建请求中心任务时按 `marketplace + sellerName` 去重，只抓一次，优先保留命中数更高、批次更新的候选记录，避免重复消耗卖家精灵使用次数。

追加修复：

- 用户明确需要“全选所有店铺”，不只是本页；新增 `GET /api/v1/modules/shop-candidates/fetchable`，按当前筛选条件返回全部可抓候选。
- `fetchable` 只返回 `PENDING / SELECTED / FETCH_FAILED`，不会把已抓取、抓取中、已忽略、已入精品池的记录误选进请求中心任务。
- 前端新增“全选全部可抓店铺”，支持跨分页保留选择；翻页不会丢失已全选的跨页记录。
- 前端仍保留“全选本页可抓店铺”，用于小范围操作。
- 全部选择上限暂为 10000 家；达到上限时前端会提示收窄筛选条件分批处理。

生产验证：

```powershell
# 前端构建：本机 Node 内存不足时，用 Codex Node + 降低 esbuild 并发
$env:GOMAXPROCS='1'
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --max-old-space-size=4096 node_modules/vite/bin/vite.js build --mode production --minify false

docker compose -f docker-compose.prod.yml build --progress=plain java-product
docker compose -f docker-compose.prod.yml build --progress=plain frontend
docker compose -f docker-compose.prod.yml up -d java-product java-user gateway frontend
```

接口验证结果：

- `GET http://localhost:8025/api/v1/modules/shop-candidates/method-batches?methodId=M01&marketplace=UK&limit=5` 返回 200。
- UK 批次排序已变为 `2026-W28` 第一，命中商品 72 个 / 店铺 65 家。
- `GET http://localhost:8025/api/v1/modules/shop-candidates/fetchable?sourceType=METHOD_CARD&sourceCode=M01&batchCode=2026-W28&limit=10000` 返回 200，当前可抓候选 294 家。
- `SchemaGuard` 通过，生产 `prod-java-product` 正常启动。

## 请求中心批量创建 500 修复

问题现象：

- 方法卡找店单店“抓全集”可以成功。
- 全选 293 家后点击“批量抓全集”返回 `Request failed with status code 500`。

根因：

- 请求中心建任务时，前端把全部候选 id 数组写进 `sellersprite_request_run.trigger_ref`。
- 生产表 `trigger_ref` 原为 `VARCHAR(128)`，293 个 id 的 JSON 超长，MySQL 报：
  `Data too long for column 'trigger_ref' at row 1`。

修复：

- 新增迁移 `java-backend/sql/alter_sellersprite_request_run_trigger_ref_text.sql`。
- 生产库已执行：`trigger_ref` 从 `VARCHAR(128)` 改为 `TEXT`。
- 初始化 SQL `create_sellersprite_request_center_tables.sql` 已同步改为 `TEXT`，后续新环境不会复现。
- 前端 `triggerRef` 不再保存完整 id 数组，改为保存任务摘要；真实执行依赖 `sellersprite_request_item.trigger_id`。

验证：

- 临时创建 293 家 `CANDIDATE_BATCH` 请求中心任务成功返回 200。
- 临时任务写入 293 个 `sellersprite_request_item` 后已删除，不污染生产队列。
- 前端重新构建并更新 `prod-frontend`。

## 请求中心自动执行修复

问题现象：
- 请求中心页面原来的“推进”按钮实际调用 `consumeNext(runId, 1)`，点一次只处理 1 个店铺。
- 这不符合当前口径：任务创建后应由请求中心自动按卖家精灵限制执行，店铺名请求按 `ShopProductSyncService` 内部 2 秒节奏控制；前端只负责看实况、暂停、恢复、停止和失败重试。

修复内容：
- 新增 `sellerspriteRequestExecutor` 单线程执行器，确保卖家精灵请求中心全局串行，不会多个任务并发打乱 2 秒节奏和使用次数统计。
- `SellerspriteRequestCenterService.createTask` 创建任务并提交事务后自动启动后台 worker。
- `resume`、`retryItem` 会自动唤醒 worker；新增 `POST /api/v1/modules/request-center/tasks/{runId}/start` 作为手动唤醒入口。
- 应用启动后自动恢复 `PENDING/RUNNING` 请求中心任务，避免部署后任务停在运行中却无人消费。
- 启动恢复时会把 stale `RUNNING` item 放回 `PENDING`，并按 item 明细重算 run 的 `pending/success/failed/skipped/running` 计数，避免部署中断导致计数和明细不一致。
- 前端请求中心去掉“点一下处理一家”的语义，改成“唤醒自动”；页面每 3 秒自动刷新任务列表和详情抽屉。

验证：
- 前端构建通过：`vite build --mode production --minify false`。
- Java 编译通过：`docker run --rm -v "${PWD}\java-backend:/app" -v java-m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-21 mvn -pl sjzm-product -am -DskipTests compile -q`。
- 本次未创建新的真实卖家精灵抓取任务，避免额外消耗使用次数。

生产部署：
- 已执行 `docker compose -f docker-compose.prod.yml build --progress=plain java-product frontend`，Java 多模块 `BUILD SUCCESS`，前端镜像构建成功。
- 已执行 `docker compose -f docker-compose.prod.yml up -d java-product frontend`，生产 `prod-java-product`、`prod-frontend` 已更新；由于共用 `prod-java` 镜像，`prod-java-user` 和 `prod-gateway` 随依赖链同步重建。
- 部署后接口 `GET http://localhost:8025/api/v1/modules/request-center/tasks?page=1&size=1` 返回 200。
- 已确认存量任务 `REQ_233440342f9f` 自动恢复运行：`pendingCount 291 -> 289`、`successCount 2 -> 4`，后台线程 `sellersprite-request-1` 正在写入 `shop_products`。
## 店铺全集画像后端增强

围绕“店铺不是只看销量，还要看新品能力、方法卡命中和类目风险”的新口径，补齐 `shop_products` 读侧分析能力：

- `GET /api/v1/modules/shop-collection/selection-shops`：给“选品中心 > 竞品店铺”读取店铺全集画像，返回店铺销量结构、M01 命中数、90 天新品数等列表筛选字段。
- `GET /api/v1/modules/shop-collection/{marketplace}/{sellerName}/insight`：单店画像解释接口，返回快照、M01 命中、最早上架商品时间、平均上架天数、平均销量、A/B/C/D 各等级年龄和销量统计、类目结构与注意/倾向标签聚合。
- `ShopProfileSummary` 增加 M01 命中数/比例、平均上架天数、平均销量、最早上架时间、30/90/180 天新品桶、180 天以上老品桶和未知上架时间计数。
- M01 口径直接复用 `M01Rule.forMarketplace(UK/DE/US)`，不在店铺画像里另造阈值。
- 类目标签改为“注意标签 + 好品倾向标签”，不是拒绝标签。返回 `attentionLevel`：`ATTENTION_STRONG` / `ATTENTION_REVIEW` / `GOOD_TENDENCY` / `NEUTRAL` / `UNKNOWN`。
- `ATTENTION_STRONG` 只表示强注意，例如电源/插头、化学功效、安全责任、大件重货、强功效修复等信号；仍需人工结合标题、图片、价格和体积判断，不代表系统拒绝。
- `ATTENTION_REVIEW` 表示需要复核，例如小电池消费品、带液体履约、儿童、汽车、宠物、美妆接触类等；这些不能一刀切淘汰。
- `GOOD_TENDENCY` 表示类目形态贴近验证好品线，例如轻小主题品、派对礼品、DIY 手工、装饰小件、定制载体、小玩具、小配件。
- 前端优先读取 `categoryLabelStats`、`attentionLevel`、`attentionReason`、`labelMeaning`、`attentionTags`、`tendencyTags`；旧 `riskStats/riskLevel/riskReason` 仅为兼容别名，不建议新页面使用。

验证：

```powershell
docker run --rm -v "${PWD}\java-backend:/app" -v java-m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-21 mvn -pl sjzm-product -am -DskipTests compile
```

结果：`sjzm-product` 后端编译通过，`BUILD SUCCESS`。

## 店铺链路当前进度收口

截至当前，店铺分析主线已经从“方法卡命中找店”推进到“店铺全集画像解释层”：

已完成：

- 方法卡找店 -> 候选池 -> 请求中心 -> 抓店铺全集 -> `shop_products` 入库，这条主链路已成立。
- 请求中心已从“点一下推进一家”改成自动执行：任务创建后后台按 2 秒节奏跑，支持暂停、恢复、停止、失败重试。
- 候选池周批次来源已明确：M01 来源为 `competitor_products_clean.effective_week_tag`；支持全选全部可抓店铺，跨批重复按 `marketplace + sellerName` 去重抓取。
- 店铺全集画像已有 `summary / detail / products / product-wall / compare`。
- 店铺全集画像解释层后端增强已完成：M01 命中数、最早商品上架时间、平均上架天数、平均销量、A/B/C/D 各等级新品/老品分布、类目结构、注意标签/好品倾向标签。
- 标签口径已修正：不是“拒绝/禁做”，而是“注意/复核/好品倾向”，用于分析、筛选和前端解释。

仍缺：

- 前端还没接新画像接口：`selection-shops` 和 `{marketplace}/{sellerName}/insight` 需要接到“选品中心 > 竞品店铺 / 店铺画像详情”。
- 店铺商品列表还要增强筛选：按 M01 命中、新品时间段、注意标签、好品倾向标签筛商品。
- 标签规则目前是后端初版硬编码，后续应抽成独立规则类或配置化。
- M01 已接，M03/Mxx 还没完全泛化；后续需要 `method_product_hit` 或等价缓存统一承接所有方法卡。
- 精品店铺池周期复抓后的对比分析还没完整展开：需要支持按月/周复抓后看新增、消失、升级、降级和多店同时上新。
- 多店同品/相似品市场分析还没做，这是从“看一家店”升级到“看一批优质店共同验证了什么”的下一层能力。
- 本次后端增强已编译通过，但还没做生产部署和真实接口验收。

当前判断：

```text
阶段 1 / 2：主链路完成
阶段 3：方法卡找店闭环可用
阶段 4：店铺画像解释层后端已完成
下一步：前端接入 + 店铺商品筛选 + 真实接口验收
```

## 店铺三维画像模型定稿

在销量分级加入上架时间后，继续讨论确认：店铺画像不能停留在单维 A/B/C/D，也不应该只做二维“销量 × 时间”。正式模型升级为三维：

```text
销量层：A / B / C / D / UNKNOWN
时间层：新品 / 成长期 / 成熟品 / 老品 / 未知
注意与倾向层：好品倾向 / 普通 / 需复核 / 强注意 / 未知
```

三维分别回答：

- 销量层：卖得动吗？
- 时间层：是最近卖动的吗？
- 注意与倾向层：我们能不能学习复制？

关键口径：

- A/B/C/D 继续作为销量层保留，不再单独代表店铺好坏。
- 上架时间用于区分“新 A / 老 A”“新 D / 老 D”“新品 B/C / 老长尾 B/C”。
- 注意与倾向层不是“违禁/拒绝”判断，只是分析、筛选和前端解释标签。
- `ATTENTION_STRONG` 表示强注意，需要人工重点看；不代表系统拒绝。
- `GOOD_TENDENCY` 表示接近轻小、主题化、可组合、可定制的验证好品线。

三维组合解释：

- A + 新品 + 好品倾向：最近打出的强爆品，代表当前选品能力。
- A + 老品 + 好品倾向：历史利润锚点，可学习类目样貌，但不是最新机会。
- B/C + 新品 + 好品倾向：最值得挖的起量方向，可能比老 A 更重要。
- D + 新品 + 好品倾向：正常测品池，适合观察后续转化。
- A/B/C + 新品 + 强注意：有市场信号，但涉及合规、责任、体积或履约注意，需要人工判断。
- D + 老品 + 普通/强注意：低价值沉淀，通常不是优先学习对象。

已更新文档：

- `docs/店铺品级/商品分级.md`
- `docs/店铺品级/店铺画像证据层实施计划.md`
- `docs/店铺品级/竞品店铺品级-方法卡命中数方案.md`
- `docs/店铺品级/店铺全集画像标签说明.md`
- `docs/店铺品级/店铺三维画像实现计划.md`

实现计划下一步：

```text
1. 抽出标签规则类
2. 补三维矩阵聚合
3. 补商品明细三维筛选
4. 前端接 selection-shops 和 insight
5. 真实店铺数据验收
```

## 时间层桶口径冲突拍板

发现一处必须先统一的口径冲突：文档里的 `ageBucket` 是互斥时间层，但现有 `insight` SQL 里的 `new30Count / new90Count / new180Count / old180Count` 是累计窗口。

最终拍板：

- 三维画像时间层使用互斥 `ageBucket`：
  - `NEW <= 90`
  - `GROWING = 91-180`
  - `MATURE = 181-365`
  - `OLD > 365`
  - `UNKNOWN = listing_days 为空`
- 三维矩阵、店铺类型、商品筛选都必须使用互斥 `ageBucket`。
- `new30Count / new90Count / new180Count / old180Count` 保留为运营观察窗口和兼容字段，不能相加，也不能用于三维矩阵。
- `old180Count` 只是 180 天以上沉淀窗口，包含成熟品和老品，不能叫“老品数”。

已同步更新：

- `docs/店铺品级/店铺三维画像实现计划.md`
- `docs/店铺品级/店铺全集画像标签说明.md`

## 店铺三维画像审查修复与完善

对已实现的店铺三维画像做代码审查后，发现“核心可用”之外还缺几处会影响实际找店/看品的细节，本次已补齐：

- 商品明细行新增三维解释字段：
  - `ageBucket`
  - `m01Hit`
  - `attentionLevel`
  - `attentionReason`
  - `labelMeaning`
- 商品明细 SQL 返回互斥时间桶和 M01 命中状态，前端表格直接展示“时间层 / 注意倾向 / M01”。
- `attentionLevel` 筛选从“末级类目 IN 粗筛”改为逐商品按 `categoryLeaf + nodeLabelPath` 精确打标签后分页，避免同名末级类目但完整路径不同导致误带入。
- 店铺列表补三维摘要字段：
  - `newABCCount`
  - `oldDCount`
  - `goodTendencyCount`
  - `attentionStrongCount`
  - `attentionReviewCount`
  - `shopProfile3dType`
  - `shopProfile3dExplanation`
- `selection-shops` 新增筛选条件：
  - `minGoodTendencyCount`
  - `maxAttentionStrongCount`
- 前端“店铺全集画像”列表新增三维类型、新 ABC、好品倾向/强注意列，并新增好品倾向下限、强注意上限筛选。
- 请求中心页面顺手修复 `refreshTimer` 类型，减少本轮相关的 `vue-tsc` 噪音。

当前仍可继续优化：

- 列表级三维摘要当前按返回店铺逐个补算，默认 100 家没问题；如果后续要一次看几千家，应改成批量 SQL 或标签缓存表。
- 注意/倾向规则目前仍是硬编码规则类，后续应配置化并记录规则版本。
- 商品级标签当前请求时计算，后续可落到 `shop_product_label_cache` 或方法卡命中缓存层，支撑大批量筛选。
- M01 已接入，M03/Mxx 需要统一方法卡命中缓存，避免每个方法卡都在店铺画像里重复实现。

验证：

```powershell
docker run --rm -v "${PWD}\java-backend:/app" -v java-m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-21 mvn -pl sjzm-product -am "-Dtest=ShopCollectionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -q
docker run --rm -v "${PWD}\java-backend:/app" -v java-m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-21 mvn -pl sjzm-product -am "-Dtest=ShopProfileMapperXmlTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -q
docker run --rm -v "${PWD}\java-backend:/app" -v java-m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-21 mvn -pl sjzm-product -am -DskipTests compile -q
npm run build -- --minify false
```

结果：

- 店铺集合服务测试通过。
- Mapper XML 测试通过。
- Java 后端编译通过。
- 前端生产构建通过。

## 店铺三维画像二次审查优化

继续按“核心可用之外还要好用”的标准复查，补掉一个列表性能隐患：

- `ShopTierAgeCategoryCell` 增加 `marketplace / sellerName`，允许批量聚合结果按店铺回填。
- `ShopProfileMapper` 新增 `selectTierAgeCategoryCellsBatchFromShopProducts`：
  - 一次拉取一批店铺的 `销量层 × 互斥时间层 × 类目` cells。
  - 保留 `sourceRunId / batchDate / marketplace / sellerNames` 过滤，口径和单店三维 cells 一致。
- `ShopCollectionService.enrichSummary3d` 从逐店循环 SQL 改为批量 SQL + Java 分组，避免 `selection-shops / summary` 在 1000 家以上时出现 N+1 查询。
- 修正维护注释：注意/倾向层是 Java 规则补齐，不是写入 `nodeLabelPath`。
- 增加测试：
  - 列表三维摘要只走一次批量 cells 查询。
  - 批量 cells XML 必须包含 `source_run_id` 过滤。

验证：

```text
前端 npm run build -- --minify false 通过。
本机 PowerShell 没有 mvn；使用本地已有 maven:3.9-eclipse-temurin-21 一次性容器执行验证，不触碰生产容器。
ShopCollectionServiceTest + ShopProfileMapperXmlTest 通过。
sjzm-product -am -DskipTests compile 通过。
```

## 店铺总览与竞品店铺选品联动计划

继续梳理下一步前端和数据源联动，确认一个关键方向：

```text
按店铺看商品 -> 归店铺总览
按商品做筛选 -> 归选品中心
```

当前问题：

- 旧“选品中心 / 竞品店铺”里有具体商品数据，但它读取的是旧选品/竞品商品池口径。
- 新“店铺全集画像”已经有 `shop_products`、单店画像、三维筛选、商品墙、快照和对比。
- 如果继续让用户在选品中心研究某个店铺，会造成职责混乱：看店铺画像在店铺总览，看店铺商品却在选品中心。

下一步决定：

- 将“竞品店铺选品”的按店铺商品浏览能力迁移到店铺总览。
- 店铺总览形成完整链路：

```text
店铺总览列表
-> 单店画像分析
-> 店铺详情
-> 店铺商品数据详情
-> 商品墙 / 商品详情 / 历史对比
```

数据源口径：

- 店铺总览里的商品数据统一读取 `shop_products`。
- 前端调用：

```text
GET /api/v1/modules/shop-collection/{marketplace}/{sellerName}/products
```

不再读取旧 `competitor_products_clean/reference` 作为店铺商品详情主口径。

前端计划：

1. 店铺全集画像详情增加 Tab：
   - 画像总览
   - 商品数据
   - 商品墙
   - 历史对比
   - 抓取快照
2. 商品数据 Tab 接 `shopCollectionApi.shopProducts`：
   - `salesTier`
   - `ageBucket`
   - `attentionLevel`
   - `m01Only`
   - `category`
   - `keyword`
3. 选品中心商品行如果有 `sellerName / marketplace`，增加“查看店铺画像”跳转。
4. 候选池、精品池、请求中心任务详情都统一跳到店铺画像。
5. 旧 `/reference-products` 建议改名为“历史竞品商品池”或加提示，避免用户误以为它是最新店铺全集。

后端现状：

- `selection-shops` 已能提供店铺列表。
- `insight` 已能提供单店三维画像。
- `products` 已能提供单店商品明细三维筛选。
- `product-wall` 已能提供商品墙。
- `snapshots` / `compare` 已能提供快照和历史对比。
- 当前不需要新增表。

还差：

- M03/Mxx 方法卡命中未统一进入店铺画像筛选。
- 注意/倾向规则未配置化和版本化。
- 商品级标签未缓存，大批量筛选仍是请求时计算。
- 精品店铺周期复抓后的市场洞察未展开。
- 多店同品/相似品分析未接入店铺总览。
- 店铺总览与选品中心跳转参数规范未实现。
- 旧竞品店铺页与新店铺商品详情职责还没在前端完全分开。

已新增文档：

```text
docs/店铺品级/店铺总览与竞品店铺选品联动计划.md
```

## 店铺总览联动:计划审查 + 阶段 A/B 实现

先对联动计划做了一次落地审查,补进 7 条缺口/风险,再直接按阶段 A、B 实现。

### 计划文档补充(7 条)

写进 `店铺总览与竞品店铺选品联动计划.md`:

1. `reference-products` 复用 `AllSelection/index.vue`,不是独立页,加提示要按路由名判断,避免污染其它入口。
2. `ShopProfileProduct` 缺 `ageBucket`(计算列没进 select),商品数据表要展示时间层需后端补齐。
3. 注意/倾向层筛选是「先查候选再 Java 分页」,超 20000 抛异常,前端要捕获并提示收窄。
4. 抽屉 Tab 化是结构性重构,拆成 Tab 化 / 内容迁移 / 接口三小步。
5. 未抓全集判定统一用 `snapshots` 空数组信号,不用 `insight` 抛异常判断。
6. `selection-shops` 列表三维字段是请求时计算,店多时需评估缓存。
7. 阶段 C 前置「字段映射盘点」:详情组件所需字段 vs `ShopProfileProduct`。

### 后端阻塞项

- `ShopProfileProduct` 补 `listingDays`;`selectProductsFromShopProducts` select 补 `listing_days`。
- `ageBucket / m01Hit` 之前已补齐,本次确认口径。

### 阶段 A:抽屉 Tab 化 + 商品数据视图

- `shop-collection-shops` 抽屉重构为 5 Tab:画像总览 / 商品数据 / 商品墙 / 历史对比 / 抓取快照。
- 各 Tab 懒加载(`ensureTabData`,切到才拉数据),`loadCurrentSnapshotData` 只预载画像总览。
- 商品数据 Tab 即店铺商品下钻视图,三维筛选 + 时间层 / 上架天数列 + 注意层超大店提示文案。
- 抓取快照 Tab 支持切换快照;无快照显示引导抓取文案。
- 旧竞品店铺:`reference-products` 改名「历史竞品商品池」,`AllSelection` 按 `route.name==='ReferenceProducts'` 显示降级提示。

### 阶段 B:跨页面跳转

- 候选池 / 观察池 / 精品池「看画像」跳转已存在,核对路由名 `module-shop-collection-shops-ShopCollectionShops` 一致。
- `ProductDetailDialog` 新增「查看店铺画像」按钮(商品带 storeName+marketplace 时),跳转带 `tab=products`。
- 店铺画像页 `onMounted` 支持 `tab / sourceRunId` query 参数。
- `SelectionDetail` 商品不带 marketplace 字段,无法可靠跳转,未加(其店铺链接是外部亚马逊链接)。

验证:

```text
前端 vite build --mode production 通过。
ShopCollectionServiceTest + ShopProfileMapperXmlTest 7/7 通过。
sjzm-product -am 编译通过。
```

未做(留待后续):

- 阶段 C:商品详情组件字段映射盘点与复用。
- 阶段 B 跨页跳转仅核对路由名,未在浏览器实跑体验。
- 计划「还差的能力」中方法卡命中统一、规则配置化、商品级标签缓存等仍未展开。

## 店铺总览联动审查修复

继续审查阶段 A/B 的前端接线，修复 4 个会影响真实体验或后续维护的小问题：

1. 店铺全集画像页原本只在 `onMounted` 读取 query。由于布局组件以 `route.path` 作为 key，同一路由内只改 query 不会重建组件，导致从商品详情再次跳另一个店铺时可能不响应。已在 `shop-collection-shops` 增加 query watch，支持同页切换 `marketplace / sellerName / sourceRunId / tab`，并在站点变化时刷新列表。
2. `ProductDetailDialog` 跳店铺画像时不能直接把 `country` 当 `marketplace` 传给后端。已新增标准化逻辑，只输出 `UK / DE / US`，支持 `GB / USA / Germany / 英国 / 德国 / 美国` 等常见写法；无法识别则提示缺少标准市场码。
3. `ShopProfileProduct` 前端类型补 `listingDays`，与后端 DTO 和商品数据表“上架天数”列对齐。
4. 静态 `/reference-products` 路由 title 同步改为「历史竞品商品池」，并把 AllSelection 的降级提示判断改成 `route.name` 后缀匹配，兼容静态路由和模块化路由。

验证：

```text
前端 npm run build -- --minify false 通过。
```
