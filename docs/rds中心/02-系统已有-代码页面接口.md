# 02 系统已有：代码、页面、接口

下列路径在仓库里**有文件**。

## Java product：`rds` 包（业务 RDS 边界）

目录：`java-backend/sjzm-product/src/main/java/com/sjzm/product/rds/`

| 路径 | 现状 |
|------|------|
| `README.md` | 批写铁律 + 管理中心入口说明 |
| `service/RdsBatchWriteService.java` | 领星/财务/运营 **写入唯一入口** |
| `lingxing/LingxingRdsMapper.java` | 标记接口，把部分原 `mapper` 包绑到 RDS SqlSession |
| `mapper/LingxingPurchaseDataLayerMapper.java` | 采购计划/订单 |
| `mapper/LingxingInventoryBatchMapper.java` | 库存批次 |
| `mapper/LingxingShipmentDataMapper.java` | 发货 |
| `mapper/LingxingSyncCursorMapper.java` | 同步游标 |
| `mapper/OperationsLogisticsProgressMapper.java` | 运营物流进度结果 |
| `finance/mapper/FinanceRdsDailyMapper.java` | 日事实 |
| `finance/mapper/FinanceRdsUnifiedMapper.java` | 统一表 |
| `finance/mapper/FinanceRdsUnifiedPeriodMapper.java` | 统一表时间窗 |
| `finance/mapper/FinanceRdsHistoryMapper.java` | 历史 |
| `finance/mapper/FinanceRdsSellerMapper.java` | 店铺 |
| `finance/model/*.java` | 财务行模型 |
| `center/JdbcUrlInspector.java` | 解析 JDBC URL，判断是否远程 |
| `center/RdsCenterCatalog.java` | 池 + 接口静态清单 |
| `center/RdsCenterService.java` | 主库/RDS 池探测 |
| `center/RdsCenterController.java` | `GET /api/v1/modules/rds-center/overview` |

配置类（在 `config` 包，不在 `rds` 包下）：

| 路径 | 现状 |
|------|------|
| `config/PrimaryDataSourceConfig.java` | `@Primary` 主库。生产有 RDS_* 时主库也是远程 |
| `config/RdsDataSourceConfig.java` | 第二数据源，扫 `rds.mapper` + `rds.finance.mapper` |
| `config/LingxingRdsMapperScanConfig.java` | `LingxingRdsMapper` 标记的 Mapper 走 RDS 工厂 |
| `config/SchemaGuard.java` | 启动扫 `@TableName`，对**当前主库**（切 RDS 后即远程）做缺表缺列检查 |

测试：

| 路径 | 现状 |
|------|------|
| `.../rds/center/JdbcUrlInspectorTest.java` | URL 解析 |
| `.../modules/lingxing/service/LingxingFinanceRdsBatchWriteTest.java` | 财务批写 |

## Java user：登录库 + 旧「RDS 操作中心」

| 路径 | 现状 |
|------|------|
| `sjzm-user/.../RDS操作中心.md` | user 侧说明（8.22 已改「不再声称全系统只有 user 连 RDS」） |
| `controller/SystemConfigController.java` | `/api/v1/system-config`，admin，**回明文** |
| `entity/SystemConfig.java` | 对应 `ai_platform.system_config` |
| `mapper/SystemConfigMapper.java` | |
| `service/SystemConfigService.java` + `impl` | |

网关**不连库**。product 无 `/api/v1/system-config`（那条在 user）。

## Python

| 路径 | 现状 |
|------|------|
| `backend/app/config.py` | `RDS_HOST` 覆盖业务 `MYSQL_*` |
| `backend/app/api/v1/rds_center.py` | `GET /api/v1/rds-center/status`，需登录 |
| `backend/app/api/v1/__init__.py` | 已挂载上述路由 |
| `backend/app/main.py` | `app.state.mysql` 业务池；`app.state.user_mysql` 登录库池 |
| `backend/app/repositories/mysql_repo.py` | 实际连接实现 |

## 前端模块（即插即用，不改 router）

| 路径 | 现状 |
|------|------|
| `frontend/src/modules/rds-management-center/manifest.ts` | 菜单「RDS 管理中心」，order 214 |
| `frontend/src/modules/rds-management-center/index.vue` | 实况表 + 登记表 + 接口表 |
| `frontend/src/api/rdsManagement.ts` | 调 Java overview + Python status |

发版前生产前端镜像里**没有**这个菜单。

## SQL（仓库里有、给 RDS 或双库用）

| 路径 | 现状 |
|------|------|
| `java-backend/sql/rds_create_operations_logistics_tables.sql` | 8.17 补运营物流/发货表 |
| `java-backend/sql/merge_users_into_ai_platform.sql` | 账号并入 `ai_platform.users`（8.13） |
| 其它 `java-backend/sql/*.sql` | 业务 DDL，切主库到 RDS 后要对**远程**执行，SchemaGuard 认主库 |

`java-backend/sql/add_carrier_exclude_keywords.sql` 文件名不含业务 RDS 专属逻辑，只是普通 DDL。

## 接口（已写进源码）

| 方法 | 路径 | 后端 | 密码 |
|------|------|------|------|
| GET | `/api/v1/modules/rds-center/overview` | Java product | 不返回 |
| GET | `/api/v1/rds-center/status` | Python | 不返回 |
| GET | `/api/v1/system-config` 及 by-category/by-key | Java user | **返回明文，仅 admin** |

业务读写接口本身不叫 rds-center，但数据已（或发版后将）落在 RDS，清单见 [08-接口绑定清单.md](08-接口绑定清单.md)。
