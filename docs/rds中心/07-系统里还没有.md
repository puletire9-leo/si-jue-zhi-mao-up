# 07 系统里还没有

「没有」指：仓库无实现、或实现了但生产未跑起来、或云上能力未接到本系统。

## 运行态（8.22 当天）

| 缺失 | 说明 |
|------|------|
| 生产镜像未包含本次收束 | 要按 `docs/docker使用经验/部署流程.md` 发 java（含 gateway）、backend（含 celery）、frontend 后，管理中心和主库改连才生效 |
| 前端菜单未出现在已部署包 | 模块源码在 `src/modules/`，旧前端镜像扫不到 |
| 8.22 日日志 | `docs/日志/2026-08/8.22.md` 不存在 |
| 主库切 RDS 后的一次 SchemaGuard 生产实测记录 | 源码会在启动时跑；成功/失败要以发版日志为准，本文不能假装已经过关 |

## 产品能力没有

| 缺失 | 说明 |
|------|------|
| 管理中心改连接串 / 改密码 | 有意不做；改 env 再发版 |
| 管理中心展示明文密码 | 有意不做；明文只在 secrets 和 admin 的 system-config |
| 一键「本地库上传 RDS」API | 只有 shell/python 脚本 |
| 阿里云 RDS 控制台（备份、参数、监控大盘）接入本后台 | 没有 |
| 连接池 Grafana/Prometheus 专用看板 | 只有管理中心 `SELECT 1` / `isValid` |
| 热更新数据源（不停机换 host） | 没有 |
| 下线 Docker `prod-mysql` | 容器还在 compose 里，应用切走后仍可当备份，**没有**删除服务的变更 |
| gateway / nacos 连业务 RDS | 本来就不连 |
| 跨 Java+Python 的单一 `DatabaseConfig` 类 | 收束在 env，不是一个共享库 |

## 账号与同步没有

| 缺失 | 说明 |
|------|------|
| `feishu_ro` 读 `sijuelishi` | 未授权；飞书同步业务大表也未做 |
| 飞书企业版大表同步方案 | 免费版 1000 行限制仍在文档里，没有升级实施记录 |
| 双写 Docker+RDS 长期运行设计 | 迁移期脚本是一次性灌数，不是持续双写 |

## 数据口径还没做完（和连接有关，但不是连接功能）

| 缺失 | 说明 |
|------|------|
| 用 GBP 重拉覆盖 `lingxing_product_performance` 周表 | 8.22 明确不上传旧周详情 |
| 8.20 财务任务入队 | 实施记录里仍待 8.19 核对后处理 |
| 本地仍比 RDS 新、或 RDS 比本地新、未互相同步的部分领星表 | 以当时 count 脚本为准，没有自动同步器 |

## 安全债未收口

| 缺失 | 说明 |
|------|------|
| `test_rds_operations.py` 去密码 | 文件仍硬编码 |
| 管理中心对非 admin 的独立权限点 | 复用 `automation:manage`，开发角色默认没有 |
