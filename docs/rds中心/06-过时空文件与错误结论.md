# 06 过时结论、空文件、错误残留

读到下面这些时，用 [00-现状总览.md](00-现状总览.md) 覆盖，不要按字面执行。

## 开发栈已从仓库删除（2026-08-22 `2fcaf19`）

远程已去掉本地开发模式遗留，默认全部按生产：

- 删除 `docker-compose.dev.yml`、`config/public/dev.env`、`backend/Dockerfile.dev`、`scripts/startup/start_with_hot_reload.py`、`mysql/init/*` 开发初始化、`docs/development/开发规范-Dev与Prod差异.md` 等
- Java `spring.profiles.active` 默认 `prod`；JDBC 默认库 `sijuelishi`、端口 `3306`，不再用 `sijuelishi_dev` / `13338`
- Python 不再按 `ENVIRONMENT=development` 开 DEBUG/热重载
- 启动与日志命令只用 `docker-compose.prod.yml`

历史日志里的 `docker-compose.dev.yml` / `dev.env` 路径**文件已不存在**，不要再抄。

## 已经过时的结论

| 旧说法 | 出现位置（典型） | 现在 |
|--------|------------------|------|
| 全系统只有 sjzm-user 连 RDS | 8.13 文档原稿、部分日志 | user 仍是 **ai_platform 唯一 Java 入口**；product/Python 已（或发版后将）连 **sijuelishi** |
| 改 RDS 连接只重启 java-user | 同上 | 改登录库：重启 user；改业务库：发版/重启 product + backend + celery |
| RDS `sijuelishi` 是空壳、暂无服务连接 | 8.14 日志原文 | 结构早灌入；领星池早已写；8.22 已搬大部分数据 |
| 只有 java-product 用 `RDS_*` | 旧 `prod.env` 注释；`deploy-remote/.../prod.env` 仍可能如此 | 源码里 Python 也会用 `RDS_HOST` 覆盖业务池 |
| 生产主库 JDBC 只拼 `MYSQL_HOST` | 旧 application.yml / database README | product 主库已 `RDS_*` 优先 |
| 连库切换是「独立后续步骤、今天只备空壳」 | 8.14 日志 | 源码步骤已做；**生产是否切过去只看有没有发版** |

## 空文件 / 未写完

| 路径 | 现状 |
|------|------|
| `docs/架构/整体架构整理/思考整理/数据库/rds远程数据库/rds链接尽量与注意事项.md` | 0 字节。注意事项已写进本目录，不再往空文件里填 |

## 文档与现实不一致（未改正文、只在这里点名）

| 路径 | 问题 |
|------|------|
| `docs/database/README.md`「生产 MySQL binlog 保留」 | 说的是 **Docker 单机 MySQL**。阿里云 RDS 有自己的备份/binlog 策略，本仓库没有做成同一套预检 |
| `deploy-remote/config/public/prod.env` | 部署树副本，注释停留在阶段 B |
| `docs/日志/2026-08/8.14.md` | 历史正确，文末「后续慢慢迁」未回写 8.22 |

## 危险残留

| 路径 | 问题 |
|------|------|
| `scripts/test_rds_operations.py` | 硬编码 RDS 密码。现状：文件在仓库里。处理：改读 env，并视泄露情况轮换 `RDS_PASSWORD`。本文不抄密码 |

## 文件名像 RDS、实际不是

| 路径 | 说明 |
|------|------|
| `frontend/.../MetricCards.vue`、`ComboCards.vue`、方法卡相关 | 文件名/检索命中 `rds` 子串或无关，**不是**阿里云 RDS |
| `艾为系统/` 下部分文件 | 另一套系统，不是本生产链路 |
