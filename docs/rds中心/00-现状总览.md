# 00 现状总览

盘点日期：2026-08-22。以仓库代码和已执行过的迁移脚本为准，不信任口头描述。

## 一台实例，两个库

| 项 | 现状 |
|----|------|
| 云厂商 | 阿里云 RDS MySQL |
| 实例 | `rm-bp1ft07y37887765cqo` |
| 公网 | `101.37.51.239:3306`（飞书和外网脚本用这个；内网域名 VPC 内才能解析） |
| 库 `ai_platform` | 登录用户、`system_config` 凭证总账。**sjzm-user 一直在连**（`USER_MYSQL_*`） |
| 库 `sijuelishi` | 思觉智贸业务库。8.14 只灌了表结构；8.17 起领星/财务/运营走 Java 第二数据源；8.22 从 Docker `prod-mysql` 搬了大部分数据 |
| Docker `prod-mysql` | 本机容器，库名也是 `sijuelishi`。`MYSQL_*` **只给这个容器**和宿主机脚本，不要改成公网 IP |

不是两套 RDS。`USER_MYSQL_HOST` 用 RDS 域名，`RDS_HOST` 当前配的是公网 IP，指向同一台机器上的不同库。

## 三个阶段（用来判断旧文档）

| 阶段 | 大约时间 | 当时事实 | 现在还成立吗 |
|------|----------|----------|--------------|
| A 登录先上 RDS | 2026-08-13 | 只有 java-user 连 `ai_platform`；业务全在 Docker MySQL | **登录部分仍成立**；「全系统只有 user 连 RDS」**已过时** |
| B 业务库空壳 + 领星第二池 | 8.14–8.21 | RDS `sijuelishi` 先有结构；Java 另开 `RDS_*` 池写领星/财务；主库仍 Docker | **双池代码还在**；「主库仍 Docker」对**已发版生产**可能仍对，对**当前源码**已否 |
| C 业务全面改连 RDS | 2026-08-22 源码 | product 主库、Python/Celery 有 `RDS_HOST` 就覆盖到远程 `sijuelishi`；前端 RDS 管理中心 | **源码已写**；**镜像未发版则生产仍停在 B** |

## 谁应该连哪

| 进程 | 应连 | 环境变量 | 源码状态 | 生产镜像（未按 8.22 发版时） |
|------|------|----------|----------|------------------------------|
| sjzm-user / gateway 不持库 | `ai_platform` | `USER_MYSQL_*` | 已连 RDS | 已连 RDS |
| sjzm-product 主库 | `sijuelishi` 远程 | `RDS_*` 优先，否则 `MYSQL_*` | 已改 | 仍可能是 Docker |
| sjzm-product 领星/财务池 | `sijuelishi` 远程 | 一直 `RDS_*` | 已连 | 已连（若当时已配 RDS_*） |
| Python / Celery 业务 | `sijuelishi` 远程 | 启动时用 `RDS_*` 覆盖 `MYSQL_*` | 已改 | 仍可能是 Docker |
| Python 用户中心池 | `ai_platform` | `USER_MYSQL_*` | 已有（backend 也加载 user-prod.env） | 已有 |
| Docker mysql 容器本身 | 自己 | `MYSQL_*` | 不变 | 不变 |

## 两套「中心」不要混

| 名称 | 是什么 | 有没有页面 |
|------|--------|------------|
| RDS 操作中心（旧，sjzm-user） | `GET /api/v1/system-config`，读 `ai_platform.system_config`，**含明文密码**，仅 admin | 没有独立模块页；设置/凭证查询 |
| RDS 管理中心（新，8.22） | 掩码主机、探测、接口绑定清单，**不含密码** | `frontend/src/modules/rds-management-center/`，路由 `/rds-management-center` |

## 读写铁律（业务库）

领星/财务/运营写 RDS 必须走 `RdsBatchWriteService`。主库切到同一远程实例后，主库 Mapper 仍走 `@Primary` 数据源，不要把所有写入都塞进批写中心。
