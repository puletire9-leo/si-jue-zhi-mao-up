# 04 迁移脚本（系统里有的）

迁数据和迁结构的脚本在仓库里；**没有**做成后台「一键导入本地库到 RDS」的 HTTP API。

## 8.14 结构（历史）

- 源：当时从 Docker 导出 no-data DDL（文档写 `backend/database/backup/sijuelishi_schema_20260814_clean.sql`）。
- 动作：RDS 上 `DROP/CREATE DATABASE sijuelishi` 后灌结构。
- 结果：当时 107 张空表。之后运营物流等又补过表（见下 SQL）。

## 8.17 运营物流补表

| 路径 | 现状 |
|------|------|
| `java-backend/sql/rds_create_operations_logistics_tables.sql` | `CREATE TABLE IF NOT EXISTS` 发货计划/实际、进度等 |

## 领星周链路迁 RDS（更早的业务迁数）

| 路径 | 现状 |
|------|------|
| `scripts/migrate_lingxing_weekly_to_rds.py` | 周事实幂等 upsert 到 RDS，不删 RDS 已有行 |

财务/日链路相关离线脚本还在 `scripts/lingxing_daily/`（读 RDS 或写事实），不是连接中心 UI。

## 8.22 Docker → RDS 搬业务数据

目录：`scripts/migrate-prod-mysql-to-rds/`

| 文件 | 现状 |
|------|------|
| `upload_non_lingxing.sh` | 已跑过：非领星表（约 92 张）灌入 RDS；跳过 `lingxing_*` 和当时 RDS 更新的运营进度表 |
| `upload_lingxing_except_weekly.sh` | 已跑过：登记、请求任务、采购/库存批次明细/发货计划等；**刻意不覆盖** `lingxing_product_performance`（周产品详情，币种问题要重拉） |
| `list_tables.sql` / `list_lingxing.sql` / `list_non_lingxing.sql` | 列表 |
| `count_lingxing_local.sql` / `count_lingxing_rds.sql` / `spot_counts.sql` / `verify_lingxing_moved.sql` | 对账 |

未做成 Windows 一键包装；按当时会话在环境里执行。RDS 上已有、本地更旧的日事实/统一日表等**没有用本地覆盖**。

## 本机探测脚本（有文件，用法危险）

| 路径 | 现状 |
|------|------|
| `scripts/test_rds_operations.py` | 用 pymysql 探运营物流表。**源码里写死了主机和密码**，违反「密钥只放 secrets」。不要再复制密码；应改为读环境变量。本中心文档不转载该密码 |

## 离线 env

`config/secrets/finance_rds.env`：财务脚本直连 RDS，compose **不**注入。
