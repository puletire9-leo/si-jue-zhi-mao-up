# 月度模型测试

按以下顺序查看：

1. `团队SKU_月度上架批次模型测试报告_数据截止2026-06.md`：先看结论、口径和限制。
2. `团队SKU_月度上架批次模型测试_数据截止2026-06.xlsx`：按上架批次查看汇总与全部店铺-SKU 阶段。
3. `团队SKU_月度上架批次阶段明细_数据截止2026-06.csv`：供后续模型或程序读取。

本轮测试以首次 FBA 可售月划分上架批次，使用随后两个完整自然月的销量判断：首月有无出单、第二月是否持续出单。它不把库存快照当作 Q1，也不把结算毛利润称作真实净利润。

所有字段均为中文，具体含义见 `字段说明.md`。
