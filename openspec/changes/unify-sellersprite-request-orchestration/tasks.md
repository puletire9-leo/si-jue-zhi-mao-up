## 1. 数据模型与迁移

- [x] 1.1 盘点并定义请求中心运行、子项、调用审计和全局门禁的字段及状态枚举。
- [x] 1.2 为 `sellersprite_request_run` 增加系统暂停原因、恢复时间和来源幂等关联字段，并编写幂等 SQL 迁移。
- [x] 1.3 为 `sellersprite_request_item` 增加 payload、尝试次数、下次重试时间、错误代码、错误摘要、请求发出和使用次数确认字段，并编写幂等 SQL 迁移。
- [x] 1.4 扩展或新增调用审计表，记录 run/item、请求范围、尝试结果、错误分类和使用次数确认状态。
- [x] 1.5 更新实体、Mapper、SchemaGuard 基础建表 SQL 和请求中心状态常量；验证新旧数据库迁移均可通过。

## 2. 统一卖家精灵执行网关

- [x] 2.1 创建 `SellerspriteExecutionGateway` 及类型化请求、响应、错误代码和调用上下文模型。
- [x] 2.2 将唯一 `HttpClient`、密钥读取、超时、请求脱敏日志和调用审计迁入执行网关。
- [x] 2.3 实现网络、超时、熔断、限流、鉴权、参数和上游业务错误的分类与原始错误摘要保留。
- [x] 2.4 使用 Redisson 实现跨 worker 的分钟配额门禁，并与现有月度用量统计对齐。
- [x] 2.5 将 Resilience4j 熔断状态同步为请求中心可读取的全局门禁，返回恢复时间和触发摘要。
- [x] 2.6 实现安全重试：仅自动重试确认未发出的连接/DNS 故障和明确短期限流；结果未知请求进入等待恢复。
- [ ] 2.7 为执行网关添加单元测试，覆盖连接超时、读取超时、熔断开启、限流、鉴权失败和审计计费语义。

## 3. 请求中心恢复与保护策略

- [x] 3.1 扩展请求中心运行与子项状态机，支持 `PAUSED_SYSTEM`、`WAITING_RETRY`、手动恢复和恢复时间展示。
- [x] 3.2 修改自动消费循环：熔断或结果未知故障时暂停运行，不领取或误标后续子项。
- [x] 3.3 修改暂停、停止与多页店铺执行控制，在领取子项、每页请求和重试等待前检查控制状态。
- [x] 3.4 修改重启恢复逻辑：安全回收未完成子项，保留已发出且结果未知的人工确认状态。
- [x] 3.5 扩展请求中心详情与健康接口，返回错误分类、原始摘要、尝试次数、下次重试和全局门禁状态。
- [ ] 3.6 为请求中心增加集成测试，验证 UK 连接超时后不会继续将后续 40-ASIN 批次标为 `API_FAIL`。

## 4. 迁移现有卖家精灵消费者

- [x] 4.1 将现有 `ASIN_BATCH_LOOKUP` 消费者改为通过执行网关，保留来源任务、月份和 40-ASIN 批次语义。
- [x] 4.2 将 `ShopProductSyncService` 改为通过执行网关，并保留店铺分页、写入统计和候选池/精品池回写。
- [x] 4.3 将 `DengZongShopService` 改为通过执行网关，并确保外部失败不会被错误报告为“空店铺同步成功”。
- [x] 4.4 将 `AsinImportService` 的旧 ASIN 与卖家名直连 HTTP 实现替换为请求中心任务创建/兼容代理。
- [x] 4.5 移除或禁用上述服务中的重复 `HttpClient`、独立限速和重复调用审计代码。

## 5. 统一业务入口与兼容迁移

- [x] 5.1 新增手动 ASIN、通用竞品查询、卖家名批量和邓总店铺同步的请求中心任务创建服务及任务类型。
- [x] 5.2 将 `/api/v1/competitor/lookup` 改为创建 `MANUAL_ASIN_LOOKUP` 运行任务并返回 `runId`。
- [x] 5.3 将 `/api/v1/asin-import/execute`、失败重试和 `/seller/execute` 改为创建关联来源的请求中心任务。
- [x] 5.4 将 `/api/v1/deng-zong-shop/sync` 改为创建 `DENG_ZONG_SHOP_SYNC` 运行任务。
- [x] 5.5 将候选池单店与旧批量确认抓取入口改为创建请求中心任务或返回明确迁移提示。
- [x] 5.6 为来源 ASIN、候选记录、精品店和店铺目标建立活跃运行幂等保护，避免迁移期重复扣费。
- [x] 5.7 更新 Controller OpenAPI 描述、网关/nginx 路由和部署预检规则。

## 6. 前端统一体验

- [x] 6.1 更新请求中心任务类型、状态标签、筛选器和详情，展示系统暂停、恢复时间、错误分类、尝试次数和重试计划。
- [x] 6.2 将 ASIN 导入、历史重试、八爪鱼、店铺分析、全部选品、候选池和精品池入口统一为创建任务后跳转请求中心。
- [x] 6.3 移除旧页面对同步卖家精灵请求、旧任务轮询和“调用中即成功”的文案依赖。
- [x] 6.4 在请求中心增加卖家精灵健康/熔断状态提示，并为人工恢复和停止提供确认交互。
- [ ] 6.5 为前端任务创建、暂停恢复、错误展示和路由跳转补充组件/集成测试。
- [x] 6.6 为请求中心增加按创建月份筛选和所选月份全量 `api_calls` 汇总展示。
- [x] 6.7 对精确店名 `Amazon` 增加请求中心跳过和执行网关硬拦截，并覆盖名称归一化测试。
- [x] 6.8 为店铺名称分页抓取增加单店 10 次请求上限，超限时按部分完成继续下一店。

## 7. 验证与发布

- [x] 7.1 已完成 Java 生产镜像编译、前端生产构建与静态检查；后端测试按“除测试外全部完成”的要求跳过。
- [ ] 7.2 在测试环境模拟连接超时、读取超时、熔断开启、额度耗尽和上游业务错误，核对状态、审计和计费。
- [ ] 7.3 使用小规模 UK、US、DE ASIN 与店铺任务验证迁移后的真实请求、暂停、停止、重试和重启恢复。
- [x] 7.4 已在生产库 `sijuelishi` 执行三份幂等 SQL 迁移、完整部署预检并通过 SchemaGuard；灰度观察属于真实环境验收，按“除测试外”要求跳过。
- [x] 7.5 已下线旧同步执行器：旧 ASIN、卖家名、候选池和同步竞品 Service 入口均明确拒绝绕过请求中心的调用；运行日志同步更新。
- [x] 7.6 构建、部署并验证月份筛选与月度请求次数汇总不受分页影响。
- [x] 7.7 构建、部署并验证 `Amazon` 子项不发出请求、不增加使用次数且不阻塞后续店铺。
- [x] 7.8 构建、部署并验证单店不会发出第 11 次请求，且后续店铺仍会继续消费。
