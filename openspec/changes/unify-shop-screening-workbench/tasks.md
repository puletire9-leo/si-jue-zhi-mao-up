## 1. 统一查询模型

- [x] 1.1 新增店铺筛选请求/响应 DTO，覆盖周批次、批量店铺、商品条件、店铺条件、排序和分页。
- [x] 1.2 新增 `shop_products` 店铺聚合分页 Mapper，数据库内完成多周去重、商品筛选、通过数/M01/平均上架时间聚合。
- [x] 1.3 新增统一店铺筛选 Service 与 Controller API，并返回正确总店铺数和 KPI 汇总。
- [x] 1.4 新增周批次选项 API，统一返回 `batchCode`、店铺数、商品数和最新周标识。

## 2. 性能与观察池

- [x] 2.1 将正式观察池改为服务端分页，并支持站点、状态、来源、周批次和店铺搜索。
- [x] 2.2 为统一查询和观察池补充幂等组合索引迁移及 SchemaGuard 检查。
- [x] 2.3 使用生产数据执行查询计划和接口耗时验证，确认默认/复杂查询达到性能目标。

## 3. 前端统一筛选框架

- [x] 3.1 新增共享店铺筛选状态与筛选组件，复用周批次、多项搜索和新品榜范围筛选字段。
- [x] 3.2 店铺详情列表切换统一分页 API，移除固定 limit 和前端二次筛选。
- [x] 3.3 正式观察池切换服务端分页，并展示通过筛选商品数、M01 命中和平均上架天数。
- [x] 3.4 店铺画像主列表切换 `shop_products` 周批次工作台，移除日期批次和旧 `deng_zong_shop` 数量口径。
- [x] 3.5 三页支持按通过筛选商品数、M01 命中、平均上架天数、商品数和新品数排序。

## 4. 验证与发布

- [ ] 4.1 增加后端聚合/分页测试和前端筛选参数测试。
- [x] 4.2 完成 Java 镜像、前端构建、部署预检和生产部署。
- [x] 4.3 在 UK/US/DE 核对三页周批次、店铺总数和筛选结果一致，不再出现 28/100 条错误截断。
