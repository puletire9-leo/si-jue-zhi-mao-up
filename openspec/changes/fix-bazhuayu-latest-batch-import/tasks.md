## 1. API 事实确认与止损基线

- [ ] 1.1 用现有八爪鱼官方文档和只读请求确认 `/data/all` 的排序、offset、size、total、restTotal 语义，以及是否存在返回历史执行 `lotNo` 的接口
- [ ] 1.2 保存一个“最新批次 50,090 / 累计 287,753”的脱敏响应样本作为回归 fixture，不保存令牌或业务敏感行数据
- [ ] 1.3 在 `BazhuayuClientDrainTest` 增加累计总量大于批次数量的红灯测试，证明当前实现会错误读取累计数据
- [ ] 1.4 明确受限快照启用条件；若无法确认 `/data/all` 最新优先契约，则将无 `lotNo` 导入默认改为拒绝并只开放精确模式

## 2. Java 批次下载修复

- [ ] 2.1 为批次下载引入显式 `EXACT_LOT_NO` / `LATEST_BOUNDED_SNAPSHOT` 模式，移除通过空 `lotNo` 隐式选择历史全量的分支
- [ ] 2.2 修改 `fetchBatchDataStreaming`：精确模式使用 `/data/lotno/all.total`，受限模式固定使用 `cloudCount` 作为 N，`/data/all.total` 仅记录为累计诊断值
- [ ] 2.3 实现最后一页裁剪、达到 N 立即停止、空页、游标不前进、restTotal 提前结束和响应结构异常的失败关闭检查
- [ ] 2.4 保证 Controller 接收页面批次时校验一次，异步 worker 首次写入前再次校验；批次变化不得切换到新批次
- [ ] 2.5 确保系统主动 start 的真实 `lotNo` 贯穿 run state、批次快照、下载请求和 `asin_import_tasks.bazhuayu_lot_no`
- [ ] 2.6 统一失败收口：异常调用 `failStreamingTask`，记录实际已读数量，不调用 `finishStreamingTask`，也不产生 READY 假状态
- [ ] 2.7 保持 `(bazhuayu_mapping_id, bazhuayu_batch_no)` 幂等约束和并发保护，重复点击返回既有任务或已导入结果

## 3. 前端批次交互与可观测性

- [ ] 3.1 扩展八爪鱼 API 类型，返回导入模式、目标数量、累计总量、实际读取数量和结构化失败代码
- [ ] 3.2 在任务配置页持续展示页面确认的批次号、开始/结束时间、批次数量和 Finished 状态
- [ ] 3.3 显示“精确批次”或“最新快照（限本批次数量）”，提交期间锁定该行操作，避免重复点击
- [ ] 3.4 对批次变化、无可靠边界、分页短读和重复导入分别提供明确提示，并提供“刷新批次”或“系统启动采集”的下一步
- [ ] 3.5 确保旧前端缺少新增字段时仍由后端执行全部边界保护

## 4. 自动化测试

- [ ] 4.1 覆盖无 `lotNo` 且 `total=287753/N=50090` 时恰好读取 N 条并停止，不读取第 N+1 条
- [ ] 4.2 覆盖最后一页大于 remaining 时裁剪、N 为 0、空页、短页、restTotal 提前归零和 offset 不前进
- [ ] 4.3 覆盖真实 `lotNo` 仅请求 `/data/lotno/all`，且精确存量少于 cloudCount 时不从其他批次补数
- [ ] 4.4 覆盖页面提交后批次变化、状态非 Finished、起止时间变化和数量变化时零写入失败
- [ ] 4.5 覆盖中途异常进入 ERROR、成功收口未调用、已处理数量可审计以及同批次重复点击幂等
- [ ] 4.6 补充前端模式标签、按钮锁定和错误提示测试，并运行 Java bazhuayu 定向测试与前端类型/构建检查

## 5. 文档、发布与生产验收

- [ ] 5.1 更新 `java-backend/AGENTS.md`，删除“无 lotNo 按 `/data/all.total` 实际存量导入”的错误规则，写入批次数量硬上限和失败关闭约束
- [ ] 5.2 更新 `docs/八爪鱼使用/榜单链接采榜单数据.md` 的接口策略、模式差异、限制、排错和操作流程
- [ ] 5.3 在当日变更日志记录根因、修复策略、测试结果和仍然存在的开放 API 限制
- [ ] 5.4 开发环境用累计大于批次的 fixture 验证 API 请求次数、写入数、任务统计和前端展示
- [ ] 5.5 生产发布前备份相关任务表并确认没有进行中的同配置导入任务；先部署后端保护，再部署前端
- [ ] 5.6 使用一个小批次做受控验收：核对八爪鱼批次数量、应用实际读取数、数据库 raw/pass/fail 数、批次字段和日志完全对应
- [ ] 5.7 监控 24 小时内批次导入耗时、API 页数、短读错误、重复任务和数据库写入量；异常时禁用无 `lotNo` 导入而不是恢复全量回退
