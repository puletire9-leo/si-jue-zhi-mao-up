- 好像在BSR选品管理（运营）里面做过校验，如果变体没有工厂链接组合，就自动删除
    - 但是这个逻辑没有在BSR选品管理里做，导致不一致，看着很怪
    - 这个逻辑应该是阻止提交表单，让用户补充完整后才能提交，而不是偷偷删掉现有数据，这样让用户看着很奇怪
    - 2026-02-06已改成阻止提交表单，让用户补充完整后才能提交

- 工厂管理，变体管理当前太粗糙了，只是两个json字段
    - 连个ID都没有，一旦这些发生变化，就无法索引了
    - 2026-02-02先改成UUID来标识某条记录了，可能后续改成关系数据好一些
    - 应该使用伪删除逻辑
    - 2026-02-03已改成双写和伪删除逻辑，为兼容其他模块在选品管理里面仍然冗余JSON字段，后续看看全部改成用关联表，再清理冗余字段

- 用户列表，不显示角色问题
    - 2026-02-27已修复

- bsr选品页面之间完全没有复用，如果某些相似的地方，改一个就要改其他的

- 前端读取后端接口列表文件EPS，目前没有在CI中动态生成，依赖build/dist/中的eps文件，如果没在本地跑缓存，EPS可能出错，后续需要看看怎么在CI中生成