<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sjzm.product.mapper.MethodCardMapper">

    <sql id="M01Where">
        cp.marketplace = #{marketplace}
        AND cp.title IS NOT NULL
        AND (
            cp.source LIKE '%新品%'
            OR LOWER(cp.source) LIKE '%new%'
        )
        <if test="month != null and month != ''">
            AND cp.month = #{month}
        </if>
        <if test="effectiveWeekTags != null and !effectiveWeekTags.isEmpty()">
            AND cp.effective_week_tag IN
            <foreach collection="effectiveWeekTags" item="weekTag" open="(" separator="," close=")">
                #{weekTag}
            </foreach>
        </if>
        <if test="bsrId != null and bsrId != ''">
            AND cp.bsr_id = #{bsrId}
        </if>
        <if test="nodeId != null">
            AND cp.node_id = #{nodeId}
        </if>
        AND cp.price BETWEEN #{priceMin} AND #{priceMax}
        AND cp.weight_g IS NOT NULL
        AND cp.weight_g &lt; #{weightMax}
        AND cp.listing_days IS NOT NULL
        AND cp.listing_days &lt; #{listingDaysMax}
        AND (
            (cp.listing_days &lt;= 30 AND cp.units IS NOT NULL AND cp.units &gt;= #{sales30})
            OR (cp.listing_days &lt;= 60 AND cp.units IS NOT NULL AND cp.units &gt;= #{sales60})
            OR (cp.listing_days &lt;= 90 AND cp.units IS NOT NULL AND cp.units &gt;= #{sales90})
            <if test="bsrMax != null">
                OR (cp.bsr IS NOT NULL AND cp.bsr &gt; 0 AND cp.bsr &lt; #{bsrMax})
            </if>
        )
    </sql>

    <select id="selectLatestM01EffectiveWeek" resultType="java.lang.String">
        SELECT cp.effective_week_tag
        FROM competitor_products_clean cp
        WHERE cp.marketplace = #{marketplace}
          AND cp.effective_week_tag IS NOT NULL
          AND (
              cp.source LIKE '%新品%'
              OR LOWER(cp.source) LIKE '%new%'
          )
        GROUP BY cp.effective_week_tag
        ORDER BY MAX(cp.created_at) DESC, cp.effective_week_tag DESC
        LIMIT 1
    </select>

    <select id="countM01Products" resultType="long">
        SELECT COUNT(1)
        FROM competitor_products_clean cp
        WHERE <include refid="M01Where" />
    </select>

    <select id="selectM01Products" resultType="com.sjzm.product.dto.MethodCardProductResponse">
        SELECT
            cp.id,
            cp.marketplace,
            cp.asin,
            cp.parent_asin,
            cp.dedup_key,
            cp.month,
            cp.effective_week_tag,
            cp.effective_week_tag AS created_week,
            cp.title,
            cp.brand,
            cp.image_url,
            cp.node_id,
            cp.node_label_path,
            cp.bsr_id,
            cp.units,
            cp.bsr,
            cp.price,
            cp.listing_days,
            <!-- 上架日 bigint(毫秒) → 直接格式化为 yyyy-MM-dd 字符串, 对齐 CompetitorProductResponse.availableDate -->
            DATE_FORMAT(FROM_UNIXTIME(cp.available_date / 1000), '%Y-%m-%d') AS available_date,
            cp.weight_g,
            cp.rating,
            cp.ratings,
            cp.fulfillment,
            cp.source,
            cp.product_url,
            cp.similar_url,
            cp.created_at
        FROM competitor_products_clean cp
        WHERE <include refid="M01Where" />
        ORDER BY
            CASE WHEN cp.bsr IS NOT NULL AND cp.bsr &gt; 0 THEN 0 ELSE 1 END ASC,
            cp.bsr ASC,
            cp.units DESC,
            cp.asin ASC
        LIMIT #{offset}, #{size}
    </select>

    <sql id="M02Where">
        dz.marketplace = #{marketplace}
        AND dz.asin IS NOT NULL
        AND dz.title IS NOT NULL
        <if test="month != null and month != ''">
            AND dz.month = #{month}
        </if>
        <if test="batchDate != null and batchDate != ''">
            AND dz.batch_date = #{batchDate}
        </if>
        <if test="bsrId != null and bsrId != ''">
            AND dz.bsr_id = #{bsrId}
        </if>
        <if test="nodeId != null">
            AND dz.node_id = #{nodeId}
        </if>
    </sql>

    <select id="selectLatestM02BatchDate" resultType="java.lang.String">
        SELECT MAX(dz.batch_date)
        FROM deng_zong_shop dz
        WHERE dz.marketplace = #{marketplace}
          AND dz.batch_date IS NOT NULL
    </select>

    <select id="countM02Products" resultType="long">
        SELECT COUNT(1)
        FROM (
            SELECT dz.asin,
                   ROW_NUMBER() OVER (PARTITION BY dz.asin ORDER BY dz.created_at DESC, dz.id DESC) AS rn
            FROM deng_zong_shop dz
            WHERE <include refid="M02Where" />
        ) t
        WHERE t.rn = 1
    </select>

    <select id="selectM02Products" resultType="com.sjzm.product.dto.MethodCardProductResponse">
        SELECT *
        FROM (
            SELECT
                dz.id,
                dz.marketplace,
                dz.asin,
                dz.parent_asin,
                NULL AS dedup_key,
                dz.month,
                dz.batch_date AS effective_week_tag,
                dz.batch_date AS created_week,
                dz.title,
                dz.brand,
                dz.image_url,
                dz.node_id,
                dz.node_label_path,
                dz.bsr_id,
                dz.units,
                dz.bsr,
                dz.price,
                NULL AS listing_days,
                <!-- 上架日 bigint(毫秒) → yyyy-MM-dd, 与 M01/竞品 DTO 对齐 -->
                DATE_FORMAT(FROM_UNIXTIME(dz.available_date / 1000), '%Y-%m-%d') AS available_date,
                NULL AS weight_g,
                dz.rating,
                dz.ratings,
                dz.fulfillment,
                COALESCE(dz.source, '郑总同行') AS source,
                dz.product_url,
                dz.similar_url,
                dz.created_at,
                ROW_NUMBER() OVER (PARTITION BY dz.asin ORDER BY dz.created_at DESC, dz.id DESC) AS rn
            FROM deng_zong_shop dz
            WHERE <include refid="M02Where" />
        ) t
        WHERE t.rn = 1
        ORDER BY
            CASE WHEN t.units IS NULL THEN 1 ELSE 0 END ASC,
            t.units DESC,
            CASE WHEN t.bsr IS NULL OR t.bsr &lt;= 0 THEN 1 ELSE 0 END ASC,
            t.bsr ASC,
            t.asin ASC
        LIMIT #{offset}, #{size}
    </select>

    <!--
      ═══════════════════════════════════════════════════════════════════
      M03 FBM 自发货简单道
      与 M01/M02 严格独立: 独立 WHERE 片段, 独立 select/count, 独立最新周查询.

      判定 (三条硬门槛):
        1. fulfillment = 'FBM' (身份定义, 大小写不敏感)
        2. cp.listing_days < listingDaysMax (默认 90)
        3. cp.units >= sales90 (UK=5 / DE=10 / US=20)

      与 M01 的关键差异:
        - source 不再限「新品榜」(FBM 可能来自新品榜也可能来自竞品店铺)
        - 不看 BSR 代理 (自发货逻辑, BSR 不适用)
        - 不做价格带 / 重量过滤 (FBM 打法约束不同)
        - 不做 30/60/90 分档 (90 天单段门槛, 见方法卡文档〔依据〕)
      ═══════════════════════════════════════════════════════════════════
    -->
    <sql id="M03Where">
        cp.marketplace = #{marketplace}
        AND cp.title IS NOT NULL
        AND UPPER(TRIM(cp.fulfillment)) = 'FBM'
        <if test="month != null and month != ''">
            AND cp.month = #{month}
        </if>
        <if test="effectiveWeekTag != null and effectiveWeekTag != ''">
            AND cp.effective_week_tag = #{effectiveWeekTag}
        </if>
        AND cp.listing_days IS NOT NULL
        AND cp.listing_days &lt; #{listingDaysMax}
        AND cp.units IS NOT NULL
        AND cp.units &gt;= #{sales90}
    </sql>

    <select id="selectLatestM03EffectiveWeek" resultType="java.lang.String">
        SELECT cp.effective_week_tag
        FROM competitor_products_clean cp
        WHERE cp.marketplace = #{marketplace}
          AND cp.effective_week_tag IS NOT NULL
          AND UPPER(TRIM(cp.fulfillment)) = 'FBM'
        GROUP BY cp.effective_week_tag
        ORDER BY MAX(cp.created_at) DESC, cp.effective_week_tag DESC
        LIMIT 1
    </select>

    <select id="countM03Products" resultType="long">
        SELECT COUNT(1)
        FROM competitor_products_clean cp
        WHERE <include refid="M03Where" />
    </select>

    <select id="selectM03Products" resultType="com.sjzm.product.dto.MethodCardProductResponse">
        SELECT
            cp.id,
            cp.marketplace,
            cp.asin,
            cp.parent_asin,
            cp.dedup_key,
            cp.month,
            cp.effective_week_tag,
            cp.effective_week_tag AS created_week,
            cp.title,
            cp.brand,
            cp.image_url,
            cp.node_id,
            cp.node_label_path,
            cp.bsr_id,
            cp.units,
            cp.bsr,
            cp.price,
            cp.listing_days,
            DATE_FORMAT(FROM_UNIXTIME(cp.available_date / 1000), '%Y-%m-%d') AS available_date,
            cp.weight_g,
            cp.rating,
            cp.ratings,
            cp.fulfillment,
            cp.source,
            cp.product_url,
            cp.similar_url,
            cp.created_at
        FROM competitor_products_clean cp
        WHERE <include refid="M03Where" />
        ORDER BY
            cp.units DESC,
            CASE WHEN cp.listing_days IS NULL THEN 1 ELSE 0 END ASC,
            cp.listing_days ASC,
            cp.asin ASC
        LIMIT #{offset}, #{size}
    </select>

</mapper>
